# Отчёт дня 13 — состояние задачи как FSM

Прогон: 2026-09-16 11:54 UTC · режим: реальные запросы к DeepSeek (`deepseek-chat`) · модель: **deepseek-chat**

Демо-база: `task_state_demo.db` · агент `demo13` · задача `tz-portal`.

Каждая фаза — ОТДЕЛЬНЫЙ процесс (`subprocess`), поэтому отчёт проверяет главное утверждение дня: состояние задачи читается из SQLite, а не из памяти процесса.

Запуск:

```bash
cd day13
python task_state_demo.py --all           # с API-ключом
python task_state_demo.py --all --no-api  # офлайн, без сети
python task_state_demo.py --phase 2       # одна фаза
python task_state_demo.py --reset         # очистить БД и отчёт
```

## Фаза 1 — постановка задачи и пауза (pid 27120)

Задача `tz-portal` заведена: **planning/gather_requirements**, ожидаемое действие — ожидается уточнение требований пользователем.

### Шаг 1 — обычная реплика (намерения нет)

Реплика: `Собери требования к задаче`

Ответ агента (обрезан):

```text
Понял задачу — нужно собрать требования. Но пока у меня нет ни малейшего контекста: что за задача, для кого, зачем. Чтобы не выдумывать за вас, задам уточняющие вопросы. Ответьте, пожалуйста, по пунктам (можно кратко, можно развёрнуто — как удобно).

## 1. Суть задачи
- Что именно нужно сделать? Опишите своими словами в 2–3 предложениях.
- Это разработка нового продукта, доработка существующего, исследование, анализ, документ, мероприятие — что?
- Есть ли уже какие-то материалы (ТЗ, бриф, ссылки, примеры)?

## 2. Цель и результат
- Какую проблему решаем? Зачем это вообще нужно?
- Что считается успешным результатом? Как вы поймёте, что задача выполнена?
- В каком виде нужен результат (код, до
```

Состояние задачи после реплики: **planning/gather_requirements**

Блок состояния в системном промпте запроса:

```text
Состояние задачи (текущий этап и шаг; продолжай с этого места):
Текущий этап: planning. Текущий шаг: gather_requirements. Ожидаемое действие: ожидается уточнение требований пользователем. Предыдущие шаги: нет.
```

### Шаг 2 — реплика «подтверждаю» двигает шаг

Реплика: `подтверждаю`

Ответ агента (обрезан):

```text
Отлично, спасибо за подтверждение. Но чтобы двигаться дальше, мне всё-таки нужны ваши ответы на вопросы — иначе требования собирать не из чего.

Давайте упростим. Ответьте хотя бы на минимум:

1. **Что нужно сделать?** (одно-два предложения своими словами)
2. **Зачем / какой результат нужен?**
3. **Для кого?**
4. **Сроки и ограничения?**
5. **Что точно НЕ входит в задачу?**

Можно ответить свободным текстом, не по пунктам — я сам разложу по структуре.

Если удобнее — пришлите любой исходник: бриф, переписку, ссылку, черновик ТЗ. Я вытащу требования оттуда и оформлю.

С чего начнём?
```

Состояние задачи после реплики: **planning/define_scope**

Блок состояния в системном промпте запроса:

```text
Состояние задачи (текущий этап и шаг; продолжай с этого места):
Текущий этап: planning. Текущий шаг: define_scope. Ожидаемое действие: ожидается определение границ задачи агентом. Предыдущие шаги: нет.
```

Прямой вызов `advance_task_step` (кнопка «Следующий шаг»): **planning/create_plan**.

### Шаг 3 — реплика «поставь задачу на паузу»

Реплика: `поставь задачу на паузу`

Ответ агента (обрезан):

```text
Задача поставлена на паузу. ⏸️

Текущее состояние:
- **Этап:** paused
- **Шаг:** create_plan
- **Статус:** ожидание возобновления (resume)

Когда будете готовы продолжить — напишите «продолжаем» или «resume», и мы вернёмся к сбору требований с того же места. Если захотите сразу дать вводные (что за задача, цель, сроки) — тоже можно, я подхвачу.
```

Состояние задачи после реплики: **paused/create_plan**

Блок состояния в системном промпте запроса:

```text
Состояние задачи (текущий этап и шаг; продолжай с этого места):
Текущий этап: paused. Текущий шаг: create_plan. Ожидаемое действие: задача на паузе; ожидается продолжение (resume). Предыдущие шаги: нет.
```

Итог фазы (состояние ПЕРЕД перезапуском процесса): этап **paused**, шаг **create_plan** сохранён для продолжения.

После фазы 1: новый процесс (pid 27120), состояние прочитано из `task_state_demo.db` — **paused/create_plan**

## Фаза 2 — продолжение после перезапуска (pid 6380)

Состояние ДО любой реплики в этом процессе: **paused/create_plan**, `paused_from_stage` = `planning`.

Старт фазы 2: новый процесс (pid 6380), состояние прочитано из `task_state_demo.db` — **paused/create_plan**

### Шаг 1 — реплика «продолжи»

Реплика: `продолжи`

Ответ агента (обрезан):

```text
С возвращением. Продолжаем с того же места — сбор требований.

Напомню, где мы остановились: мне нужны вводные по задаче, чтобы собрать требования. Без них двигаться дальше не получится.

Ответьте, пожалуйста, хотя бы на минимум:

1. **Что нужно сделать?** — одно-два предложения своими словами
2. **Зачем / какой результат нужен?**
3. **Для кого?**
4. **Сроки и ограничения?**
5. **Что точно НЕ входит в задачу?**

Можно свободным текстом, не по пунктам — я сам разложу по структуре. Или пришлите любой исходник (бриф, переписку, ссылку, черновик ТЗ) — вытащу требования оттуда.

С чего начнём?
```

Состояние задачи после реплики: **planning/create_plan**

Блок состояния в системном промпте запроса:

```text
Состояние задачи (текущий этап и шаг; продолжай с этого места):
Текущий этап: planning. Текущий шаг: create_plan. Ожидаемое действие: ожидается утверждение плана пользователем. Предыдущие шаги: нет.
```

Прямой вызов `advance_task_step`: **execution/implement** — задача вернулась на сохранённый шаг планирования, затем пошла дальше.

## Фаза 3 — выполнение и выход на валидацию (pid 25924)

Старт фазы 3: новый процесс (pid 25924), состояние прочитано из `task_state_demo.db` — **execution/implement**

`advance_task_step`: **execution/test_locally** — локальная проверка.

### Шаг 1 — реплика «готово»

Реплика: `готово`

Ответ агента (обрезан):

```text
Вижу «готово», но пока не понимаю, к чему это относится — вводных по задаче я так и не получил. Чтобы не гадать, уточню.

Что именно готово?

- **Вариант А:** вы подготовили материалы (бриф, ТЗ, переписку) — тогда просто пришлите их, и я разберу.
- **Вариант Б:** вы готовы отвечать на вопросы — тогда начнём с первого: **что нужно сделать?** (одно-два предложения своими словами).
- **Вариант В:** вы имели в виду что-то другое — напишите, что.

Без этого я не могу собрать требования — двигаться дальше будет наугад. Жду ваш ответ.
```

Состояние задачи после реплики: **validation/review**

Блок состояния в системном промпте запроса:

```text
Состояние задачи (текущий этап и шаг; продолжай с этого места):
Текущий этап: validation. Текущий шаг: review. Ожидаемое действие: ожидается ревью результата пользователем. Предыдущие шаги: planning (gather_requirements, define_scope, create_plan) — завершены; execution (implement, test_locally) — завершены.
```

## Фаза 4 — откат, возврат и завершение (pid 22652)

Старт фазы 4: новый процесс (pid 22652), состояние прочитано из `task_state_demo.db` — **validation/review**

`advance_task_step`: **validation/run_tests** — прогон тестов.

`rollback_task(execution)`: **execution/implement** — шаг сброшен на первый шаг этапа выполнения.

Четыре `advance_task_step` после отката: **validation/finalize** (implement → test_locally → review → run_tests → finalize).

`transition_task(done)`: **done/finalize**, задача активна: **False**.

Задача завершена — в журнале это отдельная причина, а не серия шагов.

## Фаза 5 — итог после пяти запусков (pid 18648)

Старт фазы 5: новый процесс (pid 18648), состояние прочитано из `task_state_demo.db` — **done/finalize**

Текущее состояние: **done/finalize**, ожидаемое действие — задача завершена; ожидается новая задача.

Задача в списке активных задач агента: **False** (список активных: пуст).

### Полный журнал переходов

| # | из | в | причина | время |
|---|---|---|---|---|
| 1 | — (создание) | `planning/gather_requirements` | задача создана | 2026-09-16 11:55:00 |
| 2 | `planning/gather_requirements` | `planning/define_scope` | следующий шаг | 2026-09-16 11:55:07 |
| 3 | `planning/define_scope` | `planning/create_plan` | следующий шаг | 2026-09-16 11:55:09 |
| 4 | `planning/create_plan` | `paused/create_plan` | пауза | 2026-09-16 11:55:09 |
| 5 | `paused/create_plan` | `planning/create_plan` | продолжение после паузы | 2026-09-16 11:55:13 |
| 6 | `planning/create_plan` | `execution/implement` | следующий шаг | 2026-09-16 11:55:19 |
| 7 | `execution/implement` | `execution/test_locally` | следующий шаг | 2026-09-16 11:55:21 |
| 8 | `execution/test_locally` | `validation/review` | следующий шаг | 2026-09-16 11:55:22 |
| 9 | `validation/review` | `validation/run_tests` | следующий шаг | 2026-09-16 11:55:33 |
| 10 | `validation/run_tests` | `execution/implement` | откат на предыдущий этап | 2026-09-16 11:55:33 |
| 11 | `execution/implement` | `execution/test_locally` | следующий шаг | 2026-09-16 11:55:33 |
| 12 | `execution/test_locally` | `validation/review` | следующий шаг | 2026-09-16 11:55:33 |
| 13 | `validation/review` | `validation/run_tests` | следующий шаг | 2026-09-16 11:55:33 |
| 14 | `validation/run_tests` | `validation/finalize` | следующий шаг | 2026-09-16 11:55:33 |
| 15 | `validation/finalize` | `done/finalize` | задача завершена | 2026-09-16 11:55:33 |

Всего переходов в журнале: **15**.

### Вывод

Состояние задачи сохранено между пятью запусками процесса: фаза 1 оставила задачу на паузе в `paused/create_plan`, фаза 2 в новом процессе продолжила её с этого же шага, фазы 3–4 довели до `done`, а фаза 5 прочитала итог из `task_state_demo.db`. Ни один процесс не держал состояние в памяти: всё, что видно в журнале, — строки таблиц `task_states` и `task_transitions`.

Воспроизведение:

```bash
cd day13
python task_state_demo.py --all           # нужен DEEPSEEK_API_KEY в day13/.env
python task_state_demo.py --all --no-api  # офлайн-заглушка, без сети
```

