# Отчёт дня 13 — состояние задачи как FSM

Прогон: 2026-09-16 12:56 UTC · режим: офлайн-заглушка (`--no-api`, без сети) · модель: **deepseek-chat**

Демо-база: `task_state_demo.db` · агент `demo13` · задача `tz-portal`.

Каждая фаза — ОТДЕЛЬНЫЙ процесс (`subprocess`), поэтому отчёт проверяет главное утверждение дня: состояние задачи читается из SQLite, а не из памяти процесса.

Запуск:

```bash
cd day13
uv run python scripts/task_state_demo.py --all           # с API-ключом
uv run python scripts/task_state_demo.py --all --no-api  # офлайн, без сети
uv run python scripts/task_state_demo.py --phase 2       # одна фаза
uv run python scripts/task_state_demo.py --reset         # очистить БД и отчёт
```

## Фаза 1 — постановка задачи и пауза (pid 24028)

Задача `tz-portal` заведена: **planning/gather_requirements**, ожидаемое действие — ожидается уточнение требований пользователем.

### Шаг 1 — обычная реплика (намерения нет)

Реплика: `Собери требования к задаче`

Ответ агента (обрезан):

```text
[офлайн-заглушка] запрос: Собери требования к задаче
[офлайн-заглушка] профиль в промпте: Текущий этап: planning. Текущий шаг: gather_requirements. Ожидаемое действие: ожидается уточнение требований пользователем. Предыдущие шаги: нет.
```

Состояние задачи после реплики: **planning/gather_requirements**

Блок состояния в системном промпте запроса:

```text
Состояние задачи (текущий этап и шаг; продолжай с этого места):
Текущий этап: planning. Текущий шаг: gather_requirements. Ожидаемое действие: ожидается уточнение требований пользователем. Предыдущие шаги: нет.
```

### Шаг 2 — реплика «подтверждаю» двигает шаг

Реплика: `подтверждаю`

Ответ агента (обрезан):

```text
[офлайн-заглушка] запрос: подтверждаю
[офлайн-заглушка] профиль в промпте: Текущий этап: planning. Текущий шаг: define_scope. Ожидаемое действие: ожидается определение границ задачи агентом. Предыдущие шаги: нет.
```

Состояние задачи после реплики: **planning/define_scope**

Блок состояния в системном промпте запроса:

```text
Состояние задачи (текущий этап и шаг; продолжай с этого места):
Текущий этап: planning. Текущий шаг: define_scope. Ожидаемое действие: ожидается определение границ задачи агентом. Предыдущие шаги: нет.
```

Прямой вызов `advance_task_step` (кнопка «Следующий шаг»): **planning/create_plan**.

### Шаг 3 — реплика «поставь задачу на паузу»

Реплика: `поставь задачу на паузу`

Ответ агента (обрезан):

```text
[офлайн-заглушка] запрос: поставь задачу на паузу
[офлайн-заглушка] профиль в промпте: Текущий этап: paused. Текущий шаг: create_plan. Ожидаемое действие: задача на паузе; ожидается продолжение (resume). Предыдущие шаги: нет.
```

Состояние задачи после реплики: **paused/create_plan**

Блок состояния в системном промпте запроса:

```text
Состояние задачи (текущий этап и шаг; продолжай с этого места):
Текущий этап: paused. Текущий шаг: create_plan. Ожидаемое действие: задача на паузе; ожидается продолжение (resume). Предыдущие шаги: нет.
```

Итог фазы (состояние ПЕРЕД перезапуском процесса): этап **paused**, шаг **create_plan** сохранён для продолжения.

После фазы 1: новый процесс (pid 24028), состояние прочитано из `task_state_demo.db` — **paused/create_plan**

## Фаза 2 — продолжение после перезапуска (pid 22212)

Состояние ДО любой реплики в этом процессе: **paused/create_plan**, `paused_from_stage` = `planning`.

Старт фазы 2: новый процесс (pid 22212), состояние прочитано из `task_state_demo.db` — **paused/create_plan**

### Шаг 1 — реплика «продолжи»

Реплика: `продолжи`

Ответ агента (обрезан):

```text
[офлайн-заглушка] запрос: продолжи
[офлайн-заглушка] профиль в промпте: Текущий этап: planning. Текущий шаг: create_plan. Ожидаемое действие: ожидается утверждение плана пользователем. Предыдущие шаги: нет.
```

Состояние задачи после реплики: **planning/create_plan**

Блок состояния в системном промпте запроса:

```text
Состояние задачи (текущий этап и шаг; продолжай с этого места):
Текущий этап: planning. Текущий шаг: create_plan. Ожидаемое действие: ожидается утверждение плана пользователем. Предыдущие шаги: нет.
```

Прямой вызов `advance_task_step`: **execution/implement** — задача вернулась на сохранённый шаг планирования, затем пошла дальше.

## Фаза 3 — выполнение и выход на валидацию (pid 6244)

Старт фазы 3: новый процесс (pid 6244), состояние прочитано из `task_state_demo.db` — **execution/implement**

`advance_task_step`: **execution/test_locally** — локальная проверка.

### Шаг 1 — реплика «готово»

Реплика: `готово`

Ответ агента (обрезан):

```text
[офлайн-заглушка] запрос: готово
[офлайн-заглушка] профиль в промпте: Текущий этап: validation. Текущий шаг: review. Ожидаемое действие: ожидается ревью результата пользователем. Предыдущие шаги: planning (gather_requirements, define_scope, create_plan) — завершены; execution (implement, test_locally) — завершены.
```

Состояние задачи после реплики: **validation/review**

Блок состояния в системном промпте запроса:

```text
Состояние задачи (текущий этап и шаг; продолжай с этого места):
Текущий этап: validation. Текущий шаг: review. Ожидаемое действие: ожидается ревью результата пользователем. Предыдущие шаги: planning (gather_requirements, define_scope, create_plan) — завершены; execution (implement, test_locally) — завершены.
```

## Фаза 4 — откат, возврат и завершение (pid 11972)

Старт фазы 4: новый процесс (pid 11972), состояние прочитано из `task_state_demo.db` — **validation/review**

`advance_task_step`: **validation/run_tests** — прогон тестов.

`rollback_task(execution)`: **execution/implement** — шаг сброшен на первый шаг этапа выполнения.

Четыре `advance_task_step` после отката: **validation/finalize** (implement → test_locally → review → run_tests → finalize).

`transition_task(done)`: **done/finalize**, задача активна: **False**.

Задача завершена — в журнале это отдельная причина, а не серия шагов.

## Фаза 5 — итог после пяти запусков (pid 18212)

Старт фазы 5: новый процесс (pid 18212), состояние прочитано из `task_state_demo.db` — **done/finalize**

Текущее состояние: **done/finalize**, ожидаемое действие — задача завершена; ожидается новая задача.

Задача в списке активных задач агента: **False** (список активных: пуст).

### Полный журнал переходов

| # | из | в | причина | время |
|---|---|---|---|---|
| 1 | — (создание) | `planning/gather_requirements` | задача создана | 2026-09-16 12:56:04 |
| 2 | `planning/gather_requirements` | `planning/define_scope` | следующий шаг | 2026-09-16 12:56:04 |
| 3 | `planning/define_scope` | `planning/create_plan` | следующий шаг | 2026-09-16 12:56:04 |
| 4 | `planning/create_plan` | `paused/create_plan` | пауза | 2026-09-16 12:56:04 |
| 5 | `paused/create_plan` | `planning/create_plan` | продолжение после паузы | 2026-09-16 12:56:06 |
| 6 | `planning/create_plan` | `execution/implement` | следующий шаг | 2026-09-16 12:56:06 |
| 7 | `execution/implement` | `execution/test_locally` | следующий шаг | 2026-09-16 12:56:08 |
| 8 | `execution/test_locally` | `validation/review` | следующий шаг | 2026-09-16 12:56:08 |
| 9 | `validation/review` | `validation/run_tests` | следующий шаг | 2026-09-16 12:56:10 |
| 10 | `validation/run_tests` | `execution/implement` | откат на предыдущий этап | 2026-09-16 12:56:10 |
| 11 | `execution/implement` | `execution/test_locally` | следующий шаг | 2026-09-16 12:56:10 |
| 12 | `execution/test_locally` | `validation/review` | следующий шаг | 2026-09-16 12:56:10 |
| 13 | `validation/review` | `validation/run_tests` | следующий шаг | 2026-09-16 12:56:10 |
| 14 | `validation/run_tests` | `validation/finalize` | следующий шаг | 2026-09-16 12:56:10 |
| 15 | `validation/finalize` | `done/finalize` | задача завершена | 2026-09-16 12:56:10 |

Всего переходов в журнале: **15**.

### Вывод

Состояние задачи сохранено между пятью запусками процесса: фаза 1 оставила задачу на паузе в `paused/create_plan`, фаза 2 в новом процессе продолжила её с этого же шага, фазы 3–4 довели до `done`, а фаза 5 прочитала итог из `task_state_demo.db`. Ни один процесс не держал состояние в памяти: всё, что видно в журнале, — строки таблиц `task_states` и `task_transitions`.

Воспроизведение:

```bash
cd day13
uv run python scripts/task_state_demo.py --all           # нужен DEEPSEEK_API_KEY в day13/.env
uv run python scripts/task_state_demo.py --all --no-api  # офлайн-заглушка, без сети
```

