# День 13 — установка и использование

Практическое руководство по приложению дня 13: **состояние задачи как конечный
автомат** — этапы `planning` → `execution` → `validation` → `done` (плюс `paused`),
шаги внутри этапа, ожидаемое действие и журнал переходов. Состояние живёт в SQLite
и подставляется отдельным блоком в системный промпт **каждого** запроса, поэтому
после перезапуска бэкенда агент продолжает с того же места без повторных
объяснений. Всё остальное — структура дня 12: **три слоя памяти** (структура дня
11 — краткосрочная, рабочая, долговременная) и **персонализация** профилем
пользователя (наследовано из дней 11–12). Здесь: установка, запуск, маршрутизация
«действие → слой», профиль и состояние задачи в интерфейсе, четыре проверки,
сценарий видео и FAQ.

Что можно делать с состоянием задачи: завести его у задачи (§7.2), вести вперёд по
шагам (§7.3), ставить на паузу (§7.4) и продолжать с того же этапа и шага (§7.5),
откатывать на предыдущий этап (§7.6) и завершать задачу (§7.7) — кнопками в
разделе «🧭 Состояние задачи», девятью эндпоинтами `/tasks...` или репликами в
чате (§7.8). Результат у всех трёх способов один: строка в `task_states` и запись
на каждый переход в `task_transitions`.

Устройство — в [architecture.md](architecture.md), контракты HTTP — в
[api.md](api.md). Главное свойство дня доказывает прогон
[`reports/task_state_demo.md`](reports/task_state_demo.md) (скрипт `scripts/task_state_demo.py`): пять
фаз в пяти **отдельных процессах** — пауза, перезапуск, продолжение с того же
шага. Персонализацию доказывает
[`reports/personalization_comparison.md`](reports/personalization_comparison.md) (скрипт
`scripts/personalization_comparison.py`, наследовано из дня 12); отчёт дня 11 по трём
слоям памяти остался в дне 11 —
[`../../day11/memory_layers_comparison.md`](../../day11/memory_layers_comparison.md)
(в `day13/` его нет).

Наследовано без изменений: слои памяти различаются не только содержимым, но и
ключом, к которому привязаны — 👤 **краткосрочная** (`short_term_messages`,
`session_id`) — реплики текущего диалога, 🗂 **рабочая** (`working_memory`,
`task_id` + `key`) — данные активной задачи, 🧠 **долговременная**
(`long_term_memory`, `category` + `key`) — профиль, предпочтения, решения, знания;
что и куда попадает — в §4. Профиль пользователя (`user_profiles`, ключ `user_id`)
— четвёртый источник системного промпта, но **не слой памяти**: он не хранит
диалог, не отбирается по релевантности (в отличие от долговременной памяти) и
подставляется в промпт целиком — см. §4.4 и §6. Состояние задачи — пятый источник
и **надстройка** над ними: оно не хранит данные задачи (они в рабочей памяти), а
фиксирует, на каком этапе и шаге задача стоит и чего система ждёт дальше (§7).

---

## 1. Установка зависимостей

Нужен **Python 3.14+**. Все команды выполняются из папки `day13` в PowerShell.

```powershell
cd day13
uv sync
```

Менеджер зависимостей — **uv**: прямые зависимости объявлены в `pyproject.toml`,
точные версии всех прямых и транзитивных пакетов — в `uv.lock`, версия
интерпретатора — в `.python-version` (`3.14`). `uv sync` создаёт `.venv` (если его
нет) и приводит его ровно к содержимому лока; отдельно создавать окружение и
активировать его не нужно — `uv run <команда>` сам находит `.venv` проекта.

Проверка, что все зависимости импортируются (одна команда, ничего не запускает):

```powershell
uv run python -c "import fastapi, uvicorn, streamlit, openai, requests, sqlalchemy, tiktoken, httpx, pytest, pandas; print('ok')"
```

Ожидаемый вывод — `ok`. Если какой-то пакет не найден, выполните `uv sync`
(или `uv run python -c` из папки `day13` — окружение проекта подхватится само).

### Состав зависимостей

Источник правды — `pyproject.toml`; точные версии прямых и транзитивных пакетов —
в `uv.lock` (файл коммитится, чтобы окружение воспроизводилось один в один).

| Пакет | Зачем он дню 13 |
|---|---|
| `fastapi>=0.111.0` | HTTP-API бэкенда, Swagger, валидация Pydantic (в том числе схемы профиля и состояния задачи) |
| `uvicorn[standard]>=0.30.0` | ASGI-сервер, на котором живёт FastAPI |
| `streamlit>=1.30.0` | веб-интерфейс: панели трёх слоёв памяти, раздел «👤 Профиль пользователя», раздел «🧭 Состояние задачи», панель сравнения профилей |
| `openai>=1.0.0` | клиент DeepSeek (совместимый с OpenAI SDK) |
| `requests>=2.31.0` | HTTP-клиент фронтенда к бэкенду |
| `sqlalchemy>=2.0.0` | ORM и работа с SQLite (агенты, три слоя памяти, конспекты, метрики, `user_profiles`, `task_states`/`task_transitions`) |
| `tiktoken>=0.8.0` | оценка числа токенов (`cl100k_base`) — в том числе по слоям |
| `httpx>=0.27.0` | транспорт для тестов FastAPI (`TestClient`) |
| `pytest>=8.0.0` | прогон тестов (в дне 13 их 584) |
| `pandas>=2.0.0` | таблицы слоёв памяти, таблицы элементов профиля, график и таблица `token_usage` в интерфейсе |

Тесты (по желанию, из папки `day13`): `uv run pytest -q`.

### Работа с `shared/`

День 13 не содержит собственной копии «междневного» кода: клиент DeepSeek, база
SQLAlchemy, подсчёт токенов и логирование берутся из пакета `shared/` в корне
репозитория (появился вместе с рефакторингом дня).

Подключение — одно место на день, `backend/__init__.py`:

```python
_REPO_ROOT = Path(__file__).resolve().parents[2]   # day13/backend/__init__.py → корень репозитория
if str(_REPO_ROOT) not in sys.path:
    sys.path.insert(0, str(_REPO_ROOT))
```

После этого импорт общих модулей выглядит обычно:

```python
from shared.deepseek_client import make_client       # клиент OpenAI SDK → DeepSeek
from shared.token_counter import count_tokens        # tiktoken, кодировка cl100k_base
from shared.db_base import Base, make_engine, init_db, make_session_factory
from shared.logging_utils import get_logger
from shared.deepseek_utils import read_key_from_env_file
```

Кто что берёт в дне 13: `backend/agents/agent.py` — `make_client` и `count_tokens`;
`backend/core/config.py` — `DEEPSEEK_BASE_URL` и `read_key_from_env_file`;
`backend/storage/database.py`, `backend/models/*.py` и `backend/models/task_state.py` —
`shared.db_base`; `backend/api/main.py`, `backend/agents/agent.py` и
`backend/storage/task_store.py` — `get_logger`.
Модули `frontend/` обращаются к бэкенду по HTTP и `shared/` не импортируют.

Дублировать этот код внутри `dayN/` нельзя (`AGENTS.md`, раздел «Запрещено»):
правка `shared/` меняет поведение всех дней сразу, поэтому её проверяют тестами
дня.

---

## 2. Переменные окружения: ключ DeepSeek

Ключ DeepSeek нужен **только бэкенду**. Фронтенд ключ не читает — он общается с
бэкендом по HTTP, поэтому вводить ключ в интерфейсе нигде не нужно.

Порядок поиска ключа:

1. файл `day13/.env` (строка `DEEPSEEK_API_KEY=...`);
2. переменная окружения `DEEPSEEK_API_KEY`.

Создайте `.env` из шаблона и впишите свой ключ:

```powershell
copy .env.example .env
```

```dotenv
# day13/.env
DEEPSEEK_API_KEY=sk-ваш-ключ
```

Файл `day13/.env` не коммитится в git. Если ключа нет:

| Без ключа | Поведение |
|---|---|
| Создание/список/правка/удаление агентов, **все операции со слоями памяти** (`/memory/...`) и **весь CRUD профилей** (`/users`, `/users/{user_id}/profile`) | работают полностью |
| `POST /agents/{id}/generate` (реальный ответ модели) | `502` со `status: "error"` и понятным текстом вроде «Ключ API не задан…»; при этом в ответе всё равно есть `profile` и `system_prompt` — блок персонализации считается до вызова DeepSeek |
| `POST /agents/{id}/compare` без `call_api` | работает: считаются только токены |

Адрес бэкенда фронтенд берёт из переменной `DAY13_BACKEND_URL`, по умолчанию
`http://127.0.0.1:8000`. Менять её нужно, если бэкенд поднят на другом порту:

```powershell
$env:DAY13_BACKEND_URL = "http://127.0.0.1:8019"
uv run streamlit run app.py
```

Скрипт отчёта читает ключ из того же `day13/.env` (или из окружения) — см. §10.

---

## 3. Запуск

Нужны **два процесса**, оба запускаются из папки `day13`.

**Терминал 1 — бэкенд (FastAPI + uvicorn, порт 8000):**

```powershell
cd day13
uv run uvicorn backend.api.main:app --port 8000
```

При старте бэкенд создаёт файл базы `day13/agents.db` (если его ещё нет) и
таблицы: восемь унаследованных (`agents`, `short_term_messages`,
`working_memory`, `long_term_memory`, `summaries`, `token_usage`, `facts`,
`checkpoints`), **`user_profiles`** — профили пользователей дня 12 и
**`task_states`** с **`task_transitions`** — состояние задачи и журнал переходов
дня 13 (всего 11 таблиц). Слои памяти, диалог, факты, ветки, метрики, профили и
состояние задач переживают перезапуск (активные сессия и задача лежат в строке
`agents`, профиль агента — в колонке `agents.user_id`, состояние задачи — в
`task_states`). Swagger — на `http://127.0.0.1:8000/docs`.

**Терминал 2 — интерфейс (Streamlit, порт 8501 по умолчанию):**

```powershell
cd day13
uv run streamlit run app.py
```

Откройте адрес, который Streamlit напечатал в консоли (обычно
`http://localhost:8501`). В сайдбаре должно появиться
«🟢 Бэкенд: http://127.0.0.1:8000 · агентов: N».

Проверка бэкенда из третьего терминала:

```powershell
curl.exe http://127.0.0.1:8000/
```

> [!] В Windows PowerShell `curl` — это алиас командлета `Invoke-WebRequest`, и
> привычные флаги (`-X`, `-H`, `-d`) у него означают другое. Примеры в этой
> документации используют **`curl.exe`** — вызывайте именно его либо работайте в
> bash-подобной оболочке (Git Bash, WSL).

`GET /` вернёт список эндпоинтов — это подтверждает, что API живой; среди полей
ответа есть `personalization` со списком эндпоинтов профиля (§6.8) и `tasks` со
списком эндпоинтов состояния задачи (§7).

### Запуск day13 после рефакторинга: что изменилось, а что нет

**Изменилась раскладка кода и путь к приложению; команды те же:**

* точка входа бэкенда — `backend.api.main:app` (было `backend.main:app`); этот
  модуль только собирает приложение (`lifespan`, CORS, `include_router`), а
  эндпоинты лежат в `backend/api/` по доменам — `agents.py` (11), `context.py` (9),
  `memory.py` (10), `profiles.py` (6), `tasks.py` (9), всего 45;
* бэкенд разложен на слои: `core/` (настройки и зависимости), `domain/` (чистые
  правила), `storage/` (доступ к БД), `services/` (суммаризация, переходы задачи),
  `agents/` (`Agent`, `MemoryManager`, `ProfileStore`, `AgentManager`), `models/`
  (ORM), `schemas/` (Pydantic-схемы), `api/` (роутеры и сборка `app`); корень
  `backend/` пуст — в нём только `__init__.py`;
* `streamlit run app.py` по-прежнему запускает фронтенд; `app.py` — точка входа
  на 45 строк, а интерфейс лежит в пакете `frontend/` (было `ui/`): `common`,
  `sidebar`, `chat_section`, `context_panels`, `memory_panels`,
  `profile_section`, `profile_comparison`, `task_panel`, HTTP-транспорт —
  `frontend/api_client.py`;
* Pydantic-схемы лежат в `backend/schemas/` (`agent`, `context`, `memory`,
  `profile`, `task`), ORM-таблицы — в `backend/models/*.py` по доменам
  (`agent`, `message`, `memory`, `context`, `user_profile`, `task_state`);
  реэкспорт таблиц — через `backend/storage/database.py`. Импорты:
  `from backend.schemas import ...`, `from backend.storage.database import ...`;
* состояние задачи разложено по обязанностям: правила FSM — `backend/domain/task_fsm.py`,
  тексты блока промпта — `backend/domain/task_prompt.py`, распознавание намерения в
  реплике — `backend/domain/task_intent.py`, работа с таблицами — `backend/storage/task_store.py`,
  переходы и валидация — `backend/services/task_state.py`, обёртки менеджера —
  `backend/agents/manager_tasks.py`;
* прогоны демонстраций — в `scripts/` (`uv run python scripts/task_state_demo.py --all`,
  `uv run python scripts/personalization_comparison.py --no-api`), их отчёты — в
  `docs/reports/` (`task_state_demo.md`, `personalization_comparison.md`);
* тесты разложены по подпапкам `tests/unit/`, `tests/integration/`, `tests/e2e/`
  (классификация по фикстурам), запускаются как раньше:
  `uv run pytest -q`;
* общий код (клиент DeepSeek, база SQLAlchemy, токены, логи) вынесен в пакет
  `shared/`; корень репозитория добавляет в `sys.path` `backend/__init__.py` —
  см. §1, «Работа с `shared/`».

Карта модулей дня и лимит размера файлов — в [`../STRUCTURE.md`](../STRUCTURE.md).

## Где искать функционал

| Задача | Файл |
|---|---|
| Настройки, лимиты, цены, пути `.env` и `agents.db` | `backend/core/config.py` |
| Доступ роутов к менеджеру (`get_manager`, `agent_or_404`, `task_or_404`) | `backend/core/dependencies.py` |
| Состояния, события и переходы состояния задачи | `backend/domain/task_fsm.py` |
| Тексты блока состояния для системного промпта | `backend/domain/task_prompt.py` |
| Распознавание намерения в реплике («пауза», «продолжи», «откат», «подтверждаю») | `backend/domain/task_intent.py` |
| Стейт-машина сжатия истории (день 9) и арифметика сжатия | `backend/domain/context_fsm.py`, `backend/domain/context_policy.py` |
| Стратегии контекста (`sliding_window`, `sticky_facts`, `branching`, `summary`) | `backend/domain/strategies.py` + ветки `_prepare_*` в `backend/agents/agent.py` |
| Извлечение фактов «ключ: значение» | `backend/domain/fact_extractor.py` |
| Категории и тексты блоков слоёв памяти | `backend/domain/memory_layers.py` |
| Значения и нормализация профиля, блок персонализации | `backend/domain/profile_values.py`, `backend/domain/profiles.py` |
| Демонстрационные профили (UI и отчёт) | `backend/domain/demo_profiles.py` |
| Переходы состояния задачи и их валидация | `backend/services/task_state.py` |
| Вызов суммаризации и запись конспекта | `backend/services/compressor.py` |
| Движок, сессии SQLite, реэкспорт ORM-классов | `backend/storage/database.py` |
| Таблицы состояния задачи и журнал переходов (чтение/запись) | `backend/storage/task_store.py` |
| Преобразования ORM-строк памяти в словари API | `backend/storage/memory_rows.py` |
| ORM-таблицы по доменам (11 таблиц) | `backend/models/*.py` |
| Pydantic-схемы API по доменам | `backend/schemas/*.py` |
| Эндпоинты по доменам и сборка приложения | `backend/api/*.py`, `backend/api/main.py` |
| Агент: слои памяти, токены, контекст, профиль, состояние задачи | `backend/agents/agent.py` |
| Три слоя памяти в SQLite (`MemoryManager`) | `backend/agents/memory.py` |
| Профиль пользователя в SQLite (`ProfileStore`) | `backend/agents/profile_store.py` |
| Пул агентов и миксины менеджера | `backend/agents/agent_manager.py`, `backend/agents/manager_*.py` |
| Интерфейс: транспорт, подписи, панели, разделы | `frontend/api_client.py`, `frontend/common.py`, `frontend/*_panels.py`, `frontend/task_panel.py`, `frontend/profile_section.py`, `frontend/sidebar.py`, `frontend/chat_section.py` |
| Прогоны демонстраций и сборка отчётов | `scripts/*.py` (отчёты — в `docs/reports/`) |
| Тесты: чистые модули / временная БД и агент / API | `tests/unit/`, `tests/integration/`, `tests/e2e/` |


### Порядок блоков в основной области

Вверху основной области — переключатель разделов (`st.radio` «Раздел», ключ
`main_section`): **«💬 Чат и память»** (открыт по умолчанию), **«👤 Профиль
пользователя»** и **«🧭 Состояние задачи»**. Это именно radio, а не `st.tabs`:
Streamlit не сохраняет выбранную вкладку между перезапусками скрипта, а после
«💾 Сохранить профиль» или перехода задачи нужен `st.rerun` — с radio
пользователь остаётся в том же разделе (§6.1, §7).

В разделе **«💬 Чат и память»** сверху вниз идёт: **карточка агента** (имя,
`agent_id`, модель, температура, `max_tokens`, стратегия и `window_size`, профиль
`user_id`; рядом — «🗑 Удалить агента») → **панель текущей стратегии** (только
одна: «🗜 Сжатие контекста» при `summary`, «📌 Факты» при `sticky_facts`,
«🌿 Ветвление истории» при `branching`) → **«📊 Токены диалога»** → **«🧠 Слои
памяти агента»** (три вкладки, §5.2–§5.4) → **«🧭 Что ушло в последний запрос»**
(§5.6) → **«💬 Диалог»** → expander «⚖️ Сравнить режимы» → форма отправки
(поле «Сообщение агенту» + кнопка «🚀 Отправить»), отдельной строкой под ней —
кнопка «🧹 Очистить историю». Сайдбар: создание агента (в том числе селектор
«Профиль пользователя (user_id)», §6.5), «⚙️ Стратегия контекста», «🗂 Задача и
сессия».

То есть панели слоёв и индикатор стоят **выше** диалога и поля ввода, а не под
ними. Порядок блоков раздела «👤 Профиль пользователя» — в §6.

В разделе **«🧭 Состояние задачи»** сверху вниз идёт: подпись «задача · агент ·
обновлено» → **этап, шаг и ожидаемое действие** (этап — с подписью из
`TASK_STAGE_LABELS`, например «⚙️ execution — выполнение») → строка о паузе (если
задача на паузе) → **ASCII-схема переходов** (`st.code`) и строка «Активный
этап» → **пять кнопок** в одну строку: «⏸ Пауза», «▶️ Продолжить»,
«⏭ Следующий шаг», «↩️ Откат на предыдущий этап», «✅ Завершить задачу»
(недоступные для текущего этапа заблокированы) → две вкладки: **«📜 Журнал
переходов»** (таблица «из → в, причина, время») и **«🧩 Блок в системном
промпте»** (ровно тот текст, что уходит в системное сообщение). Если состояния у
активной задачи ещё нет, панель пишет об этом и предлагает форму заведения
(`task_id` + начальный этап). Подробности — §7.

---

## 4. Модель памяти и профиль: что и куда сохраняется (наследовано из дней 11–12)

### 4.1 Три слоя: таблица, ключ, жизненный цикл

| Слой | Таблица | Ключ | Что хранит | Когда очищается |
|---|---|---|---|---|
| 👤 Краткосрочная | `short_term_messages` | `session_id` | реплики текущего диалога (роль + текст + время) | `new_session()`, `DELETE /agents/{id}/memory/short-term`, `DELETE /agents/{id}/history` |
| 🗂 Рабочая | `working_memory` | `task_id` + `key` | данные активной задачи: цель, ограничения, решения, критерии | вручную (перезапись ключа) или сменой задачи; новая сессия её не трогает |
| 🧠 Долговременная | `long_term_memory` | `category` + `key` | профиль, устойчивые предпочтения, важные решения, знания | только вручную: `DELETE /agents/{id}/memory/long-term/{entry_id}` |

Активные `session_id` и `task_id` агента хранит строка `agents`
(`current_session_id` / `current_task_id`) — поэтому после перезапуска бэкенда
диалог продолжается в той же сессии, а рабочая память — в той же задаче.

Профиль пользователя лежит отдельно, в таблице `user_profiles`, и привязан к
`user_id` (не к агенту и не к сессии). Колонка `agents.user_id` (по умолчанию
`"default"`) указывает, чей профиль подключать к запросам агента. Внешнего
ключа между `user_profiles` и `agents` **нет намеренно**: удаление профиля не
должно уносить агентов — они просто теряют персонализацию.

### 4.2 Правило маршрутизации: действие → слой

Это и есть ответ на «вы явно выбираете, что и куда сохраняется»: ни одна запись
не попадает в рабочую или долговременную память «сама».

| Действие | Слой | Что происходит |
|---|---|---|
| Ход диалога (`POST /agents/{id}/generate`) | краткосрочная | после успешного ответа сохраняется пара реплик `user` + `assistant` текущей сессии |
| Форма вкладки «🗂 Рабочая» (`POST /memory/working`) | рабочая | upsert по `task_id` + `key`: повторный ключ перезаписывает значение |
| `POST /memory/working` с `task_id` в теле | рабочая | запись уходит в **указанную** задачу, не обязательно в активную |
| Форма вкладки «🧠 Долговременная» (`POST /memory/long-term`) | долговременная | upsert по `category` + `key`, с уверенностью 0…1 |
| `POST /memory/session` («🆕 Новая сессия») | краткосрочная | реплики прошлой сессии (плюс конспекты `summaries` и факты `facts`) удаляются; рабочая и долговременная память остаются |
| `PUT /memory/task` («🔀 Переключить задачу») | рабочая | меняется активная задача: в контекст идут записи только новой задачи; диалог и краткосрочный слой не трогаются |
| Форма профиля (`POST`/`PUT /users/{user_id}/profile`) | — (профиль, не слой) | профиль обновляется и **сразу** применяется к живым агентам этого `user_id`; слои памяти не затрагиваются (§6) |
| Переход состояния задачи (`POST /tasks/{task_id}/advance`, `pause`, `resume`, `rollback`, `transition`) | — (состояние задачи, не слой) | меняются этап, шаг и ожидаемое действие, в `task_transitions` добавляется строка журнала; рабочая и долговременная память не затрагиваются (§7) |

**Автоматического извлечения профиля и предпочтений LLM нет.** Рабочая и
долговременная память, а также профиль наполняются только решением
пользователя — через формы вкладок, раздел профиля или соответствующие запросы
к API (что видно в §8, шаги 1 и 3, и в §6.8). Единственное, что меняется по
реплике само, — **состояние задачи**: намерение «пауза» / «продолжи» / «откат» /
«подтверждаю» распознаётся эвристикой и применяется до сборки контекста (§7.8);
данные памяти и профиль от реплик не меняются.

### 4.3 Что именно уходит в контекст

| Слой | Как попадает в запрос |
|---|---|
| 👤 Профиль пользователя | блок персонализации — **первым** в системном сообщении, если профиль не пуст (§4.4) |
| 🗂 Рабочая | **все** записи активной задачи, блоком «Рабочая память (данные текущей задачи, используй как опорные; при противоречии важнее свежая реплика):» |
| 🧠 Долговременная | до `LONG_TERM_LIMIT = 5` записей: сначала совпавшие по ключевым словам запроса или по упоминанию категории, затем добор самыми уверенными; блок «Долговременная память (профиль, предпочтения, решения, знания — устойчивые данные о пользователе):» |
| 👤 Краткосрочная | по наследованной из дня 10 стратегии: последние `window_size` реплик, последние `keep_last_messages` непокрытых конспектом или вся история активной ветки |
| 🧭 Состояние задачи | блок «Состояние задачи (текущий этап и шаг; продолжай с этого места):» — **последним** в системном сообщении, если у активной задачи заведено состояние (§7.1) |

Порядок блоков системного сообщения: **блок профиля** → `system_prompt` →
рабочая память → долговременная память → конспект (если есть) → факты (только
`sticky_facts`) → **блок состояния задачи** (если состояние заведено). Все блоки
вкладываются в **одно** системное сообщение: если ни один блок не заполнен,
отдельного system-сообщения в запросе нет вовсе.

### 4.4 Профиль в системном промпте

`Agent.__init__` читает профиль из БД по `cfg.user_id`
(`self.profile = self.profile_store.load(self.user_id)`). Профиля нет — агент
работает как обычно, без ошибки, просто без персонализации.

Блок профиля собирает `Agent._system_message(...)`, и он стоит **первым**: это
постоянная инструкция пользователя, одинаковая во всех запросах, и она не должна
теряться за блоками памяти. Так как блок — часть системного сообщения, он
попадает в контекст при **любой** стратегии (`sliding_window`, `sticky_facts`,
`branching`, `summary`) и учитывается в оценке токенов (токены блока входят в
общие `prompt_tokens` / `sent_context_tokens`; оценки трёх слоёв в `memory`
считают только слои).

Состав блока — по строке на заполненное поле, в таком порядке:

```
Профиль пользователя (персонализация; соблюдай в каждом ответе):
Обращайся к пользователю по имени: <name>.
Стиль общения: <текст по tone>
Формат ответа: <текст по format>
Длина ответа: <текст по verbosity>
Язык ответа: <текст по language>
Жёсткое ограничение: весь ответ не длиннее <N> символов.
Не обсуждай темы: <a>, <b>. Если запрос про них — вежливо откажись и предложи другую формулировку.
Всегда добавляй в ответ: <дисклеймеры>.
Дополнительные инструкции пользователя (выполняй буквально):
- <инструкция 1>
- <инструкция 2>
```

Точные тексты по значениям перечислений:

| Поле | Значение | Что попадает в промпт |
|---|---|---|
| `tone` | формальный | «на «Вы», без сленга и эмодзи, официальные формулировки» |
| `tone` | дружелюбный | «тепло и просто, уместны эмодзи и обращение к собеседнику напрямую» |
| `tone` | технический | «точные термины и конкретика, без вводных фраз, эмодзи и «воды»» |
| `format` | markdown | «заголовки, списки, блоки кода» |
| `format` | структурированный | «нумерованные разделы с подписями (например: 1. Анализ, 2. Решение, 3. Проверка)» |
| `format` | plain text | «простой текст без markdown-разметки» |
| `verbosity` | кратко | «только суть, без прелюдий и повторов» |
| `verbosity` | подробно | «с пояснениями, примерами и обоснованием» |
| `verbosity` | сбалансировано | «суть плюс короткое пояснение ключевых мест» |
| `language` | русский | «Язык ответа: русский.» |
| `language` | английский | «Язык ответа: английский — отвечай на английском.» |

Незаполненное поле (`None`, в интерфейсе — «— не задано —») в промпт не попадает
вовсе.

Чем профиль отличается от слоёв памяти:

* **краткосрочная** — реплики диалога; профиль её не читает и не пишет.
  «🆕 Новая сессия» очищает диалог, но профиль не трогает: персонализация та же;
* **рабочая** — данные задачи; `PUT /memory/task` переключает задачу, профиль не
  меняется. Блок профиля и блок рабочей памяти сосуществуют в промпте (профиль —
  раньше);
* **долговременная** — записи `profile`/`preference`/`decision`/`knowledge`,
  отбираемые по ключевым словам запроса: это **данные** для ответа, а не
  инструкции о стиле. Профиль — таблица `user_profiles` по `user_id`: одни и те
  же настройки применяются ко всем агентам пользователя, ко всем его задачам и
  сессиям, меняются на лету и не зависят от того, что попало в долговременную
  память.

---

## 5. Работа со слоями в интерфейсе (наследовано из дня 11)

### 5.1 Создать агента и задачу

Создание агента — в сайдбаре, блок **«➕ Новый агент»** (форма
`create_agent_form`, очищается после отправки): «Имя агента», «Профиль
пользователя (user_id)» (§6.5), «Модель», «Температура», «Системный промпт
(роль)», «max_tokens», «Сжимать историю в конспект», «Последних реплик «как
есть»», «Сжимать каждые N новых реплик» → кнопка «Создать агента». Новый агент
сразу становится активным; в сайдбаре появляется строка вида
`Конспектёр · deepseek-chat · 0 сообщений · 🗜 сжатие`.

Задача нужна рабочей памяти. В сайдбаре, в блоке «🗂 Задача и сессия», введите
идентификатор в поле **«Новая задача (task_id)»** (например, `tz-portal`) и
нажмите **«➕ Создать задачу»** — задача станет активной с нулём записей.
Идентификатор по умолчанию — `default`.

> [i] Это «задача» рабочей памяти: `task_id` — ключ, по которому фильтруются её
> записи. Состояние задачи (этапы и шаги) — отдельная сущность со своим
> хранилищем `task_states`: оно заводится в разделе «🧭 Состояние задачи» (§7.2) и
> привязано к **активному** `task_id` агента. Одна и та же строка сайдбара
> переключает и рабочую память, и состояние задачи: в разделе «🧭 Состояние
> задачи» видно состояние той задачи, что активна.

### 5.2 Вкладка «👤 Краткосрочная»

* Подзаголовок — **«👤 Краткосрочная память (текущая сессия)»**.
* Подпись: `Сессия **session_id** · реплик: **N** · слой очищается кнопкой
  «🆕 Новая сессия» и командой ниже.`
* Таблица — **«роль / текст / создано»** (текст обрезан до 200 символов).
* Кнопка **«🧹 Очистить краткосрочную память»** (`DELETE /memory/short-term`)
  удаляет реплики текущей сессии; рабочая и долговременная память не
  затрагиваются. Пустой слой показывает подсказку, что реплики появятся после
  первого сообщения.

### 5.3 Вкладка «🗂 Рабочая»

* Подзаголовок — **«🗂 Рабочая память (текущая задача)»**.
* Подпись: `Задача **task_id** · записей: **N** · слой переживает смену сессии и
  очищается только вручную (или сменой задачи).`
* Форма **«Добавить/обновить запись»** (ввод существующего ключа перезапишет
  значение — upsert): поля **«Ключ»** (placeholder «например: цель») и
  **«Значение»**, кнопка **«💾 Сохранить»** → `POST /memory/working`.
* Таблица — **«ключ / значение / обновлено»**.

Держите записи короткими и по делу: они уходят в **каждый** запрос задачи.

### 5.4 Вкладка «🧠 Долговременная»

* Подзаголовок — **«🧠 Долговременная память (между сессиями)»**, подпись про
  устойчивые данные о пользователе.
* Селектор **«Фильтр по категории»**: «все категории» плюс `👤 профиль`,
  `⭐ предпочтение`, `✅ решение`, `📚 знание` — фильтрует и таблицу, и селектор
  удаления.
* Форма **«Добавить/обновить запись»** (upsert по категории и ключу):
  **«Категория»**, **«Ключ»** (placeholder «например: язык_интерфейса»),
  **«Значение»** и слайдер **«Уверенность»** (0.0–1.0, шаг 0.05, по умолчанию
  1.0). Кнопка **«💾 Сохранить»** → `POST /memory/long-term`.
* Таблица — **«id / категория / ключ / значение / уверенность»**.
* Удаление: селектор **«Удалить запись по id»** + кнопка **«🗑 Удалить»**
  (`DELETE /memory/long-term/{entry_id}`); если записи нет, бэкенд отвечает
  `404`.

> [i] Записи категории `profile` в **долговременной** памяти — это обычные
> данные агента (например, «роль пользователя: аналитик»). Они не связаны с
> профилем пользователя (раздел «👤 Профиль пользователя», §6) и не влияют на
> стиль ответов: персонализация живёт в отдельной таблице `user_profiles`
> (§4.4, §6).

### 5.5 Задача и сессия (сайдбар, блок «🗂 Задача и сессия»)

* Подпись: `Сессия: **session_id** · задача: **task_id**`.
* Селектор **«Активная задача»** + кнопка **«🔀 Переключить задачу»**
  (`PUT /memory/task`): в следующих запросах фильтруется рабочая память; диалог
  и краткосрочный слой не меняются. Список задач наполняется из
  `GET /memory/working` (`tasks`).
* Поле **«Новая задача (task_id)»** + кнопка **«➕ Создать задачу»** — то же
  переключение, но на новый идентификатор.
* Кнопка **«🆕 Новая сессия»** (`POST /memory/session`): реплики прошлой сессии,
  конспекты и факты удаляются, у агента появляется новый `session_id`. **Рабочая
  и долговременная память, профиль пользователя и состояние задачи сохраняются**
  — это и показывает §9.
* Состояние задачи (день 13) привязано к тому же `task_id`: активная задача — та,
  чьё состояние показывает раздел «🧭 Состояние задачи» и чей блок уходит в
  системный промпт (§7.2). Состояния у задачи может не быть вовсе — тогда блока в
  промпте нет, а рабочая память работает как раньше.

### 5.6 Индикатор «🧭 Что ушло в последний запрос»

Заголовок **«🧭 Что ушло в последний запрос»**, сразу под панелями слоёв, читает
`record["memory"]` последнего **успешного** ответа `POST /generate`:

* три метрики — **«👤 Краткосрочная»**, **«🗂 Рабочая»**, **«🧠 Долговременная»**
  — со значением «N записей · M токенов»; подсказка метрики содержит подробности
  слоя (`details`);
* подпись: `Сессия **session_id** · задача **task_id** · всего по слоям:
  **total_tokens** токенов (короткая X + рабочая Y + долговременная Z)`;
* строка **«Использованы в запросе: …»** — только те слои, у которых
  `used = true`;
* строка **«Ключевые слова запроса (отбор долговременных записей): …»**.

До первого сообщения (и сразу после «🆕 Новая сессия») блок пишет: «Слои памяти:
индикация появится после первого сообщения». Токены самого нового промпта не
принадлежат слоям — реплика становится памятью только после успешного хода,
поэтому в **первой** реплике новой сессии краткосрочный слой даёт `0` токенов
(рабочая и долговременная при этом уже на месте).

Источник полей — `record["memory"]` ответа `POST /agents/{id}/generate`:
`session_id`, `task_id`, `layers[{layer, used, entries, tokens, details}]`,
`short_term_tokens` / `working_tokens` / `long_term_tokens` / `total_tokens`,
`keywords`. Токены блока профиля сюда не входят: они часть системного промпта
запроса — их видно в `usage.prompt_tokens` и в `record["profile"]` (§6.7).

### 5.7 Первый диалог: что ввести и что ожидать

Диалог — в самом низу раздела «💬 Чат и память»: заголовок **«💬 Диалог»**, под
ним сообщения, затем поле **«Сообщение агенту»** и кнопка **«🚀 Отправить»**
(отдельной строкой ниже — **«🧹 Очистить историю»**); между сообщениями и
полем ввода стоит expander «⚖️ Сравнить режимы» (§12). Пока агент не создан,
полей нет вовсе.

1. Создайте агента (§5.1) — поле ввода появляется сразу, никаких
   дополнительных шагов не нужно.
2. Введите первую реплику, например:
   `Привет! Начинаем собирать требования к корпоративному порталу.`
3. Нажмите **«🚀 Отправить»** (в поле работает и Ctrl+Enter) — текст уходит как
   есть, терять фокус полю не нужно.
4. Что ожидать:
   * под полем появляется плашка `Ответ получен за N с · токены … · стоимость
     … · finish_reason … · слои: короткая 0 / рабочая 0 / долговременная 0
     токенов`, а рядом — строка применённого профиля `👤 профиль <user_id>: N
     элементов` (§6.7);
   * в «💬 Диалог» добавляется пара `🧑 Вы` / `🤖 Ассистент` с временем;
   * в «🧭 Что ушло в последний запрос» появляются три метрики по слоям.

На **первой** реплике сессии краткосрочный слой даёт `0 записей · 0 токенов`
(сам новый промпт в слой ещё не входит) — это ожидаемо, а не ошибка. Со второй
реплики там `2 записи`, дальше `4`, `6` и так до `keep_last_messages` (по
умолчанию `6`). Рабочая и долговременная метрики показывают записи, сохранённые
формами вкладок: если ничего не сохраняли — там `0 записей · 0 токенов`.

Если нажать «🚀 Отправить» с пустым полем, ничего не отправляется и появляется
подпись «Пустое сообщение не отправлено: введите текст.»

Реплика может заодно сдвинуть **состояние задачи** (§7.8): «подтверждаю» — шаг
вперёд, «поставь задачу на паузу» — пауза, «продолжи» — возврат на сохранённый
этап и шаг, «вернись на предыдущий этап» — откат. Намерение применяется **до**
сборки контекста, поэтому блок состояния в системном промпте этого же запроса уже
описывает новое положение. Если состояние у задачи не заведено, задача завершена
или реплика не похожа на намерение (например, «продолжительность сессии»), запрос
уходит как обычно.

---

## 6. Профиль пользователя: создание, редактирование, переключение, кастомные инструкции (наследовано из дня 12)

### 6.1 Где живёт профиль и почему раздел, а не вкладка

Профиль — отдельный **раздел** приложения, а не вкладка внутри «💬 Чат и память».
Переключатель разделов (`st.radio` «Раздел», ключ `main_section`) стоит **вверху
основной области**: **«💬 Чат и память»**, **«👤 Профиль пользователя»** и
**«🧭 Состояние задачи»** (третий — дня 13, §7). Выбран radio, а не
`st.tabs`, потому что Streamlit не сохраняет выбор вкладки между перезапусками
скрипта, а после «💾 Сохранить профиль» выполняется `st.rerun`: с radio
пользователь остаётся в том же разделе и видит результат сохранения, с вкладками
его выбрасывало бы на первую вкладку.

Подзаголовок раздела — **«👤 Профиль пользователя»**, под ним подпись: профиль
применяется ко **всем** запросам выбранного пользователя, системный промпт
каждого запроса собирается из обращения, стиля (tone), формата (format), длины
(verbosity), языка, ограничений (constraints) и произвольных инструкций
(custom_instructions); изменения действуют сразу, перезапуск агентов не нужен.

Порядок блоков раздела сверху вниз:

1. выбор профиля («Профиль») и форма создания нового («Новый профиль: user_id»);
2. **«⚡ Готовые профили»** — три кнопки демонстрации;
3. **«✏️ Редактирование профиля `<user_id>`»** — форма, предпросмотр блока
   промпта и «🗑 Удалить профиль»;
4. **«🔀 Профиль активного агента»** (быстрое переключение) + таблица элементов
   применённого профиля + expander «Итоговый системный промпт (без блоков памяти
   задачи)»;
5. **«📊 Сравнение двух профилей на одном вопросе»** (§6.6).

### 6.2 Создать профиль

**Пустой профиль вручную.** Слева — селектор **«Профиль»** со списком из
`GET /users` (подпись варианта — `<user_id> · <summary>`), под ним строка
«Обновлён: <дата> · пользователь `user_id`». Справа — форма
**«Новый профиль: user_id»**: поле идентификатора (placeholder `ivan`,
латиница/цифры, до 64 символов) и кнопка **«➕ Создать профиль»** (`POST
/users/{user_id}/profile`). Пустое значение даёт предупреждение «Укажите user_id
нового профиля.»; занятый `user_id` — `409` (§6.8). Созданный профиль пуст («—
не задано —» во всех полях) и в промпт ничего не добавляет, пока его не
заполнят в форме редактирования (§6.3). Если профилей нет вовсе, раздел пишет:
«Профилей пока нет. Создайте первый — он начнёт влиять на запросы сразу, без
перезапуска агентов.»

**Готовые профили — одним нажатием.** Блок **«⚡ Готовые профили»** (подпись
«создаются или обновляются одним нажатием») показывает три кнопки; данные берутся
из `DEMO_PROFILES` (модуль `backend/domain/demo_profiles.py` — его импортируют и
интерфейс, и скрипт отчёта, поэтому настройки в UI и в отчёте не расходятся):

| Кнопка | `user_id` | Настройки | Чего ожидать в ответе |
|---|---|---|---|
| **Строгий технический** | `strict_tech` | имя `Инженер`, tone=технический, verbosity=кратко, language=русский, format=plain text, предел длины 600 символов | сухой ответ без разметки и вступлений: термины и план действий, не длиннее 600 символов |
| **Дружелюбный наставник** | `friendly_mentor` | имя `Илья`, tone=дружелюбный, verbosity=подробно, format=markdown, запрещённая тема «политика», инструкции «Объясняй простыми словами, используй аналогии» и «Обращайся ко мне по имени» | тёплый подробный ответ со списками и markdown-разметкой |
| **Оркестратор процесса** | `process_orchestrator` | имя `Тимлид`, tone=технический, verbosity=сбалансировано, format=структурированный, дисклеймер «Порядок ролей согласован с тимлидом», инструкции «При запросе «напиши фичу» следуй порядку: аналитик → разработчик → тестировщик» и «Всегда предлагай два варианта решения» | работа раскладывается по ролям именно в этом порядке |

Нажатие создаёт профиль, если его ещё нет, и обновляет, если он уже есть;
подсказка кнопки (`help`) показывает ожидаемый эффект и `user_id`. Для
демонстрации достаточно нажать первые две кнопки — профили с противоположными
настройками нужны панели сравнения (§6.6).

### 6.3 Отредактировать профиль

Выберите профиль в селекторе **«Профиль»** — ниже появится блок
**«✏️ Редактирование профиля `<user_id>`»** с формой (кнопка сохранения —
**«💾 Сохранить профиль»**):

| Поле формы | Что задаёт | Границы и особенности |
|---|---|---|
| **«Имя для обращения»** | строку промпта «Обращайся к пользователю по имени: …» | до 100 символов; пусто — обращения по имени нет |
| **«Стиль общения (tone)»** | тон ответов | `формальный` / `дружелюбный` / `технический` (§4.4) |
| **«Длина ответа (verbosity)»** | подробность | `кратко` / `подробно` / `сбалансировано` |
| **«Язык ответа (language)»** | язык | `русский` / `английский` |
| **«Формат ответа (format)»** | разметку | `markdown` / `plain text` / `структурированный` |
| **«Максимальная длина ответа, символов (0 — без ограничения)»** | жёсткий предел длины | 20…8000; `0` — без ограничения (в промпт строка не попадает) |
| **«Запрещённые темы (через запятую)»** | темы, которые нельзя обсуждать | до 20 тем по 100 символов; можно перечислять через запятую или точку с запятой, дубли убираются |
| **«Обязательные дисклеймеры (по одному в строке)»** | вставки в каждый ответ | до 20 штук по 500 символов |
| **«Произвольные инструкции (по одной в строке)»** | поведение агента | до 30 инструкций по 500 символов, суммарно до 4000 символов (§6.4) |

В каждом селекторе есть значение **«— не задано —»** — это `None`: поле не
навязывается и в системный промпт не попадает.

* Сохранение формы — `PUT /users/{user_id}/profile`: настройки **заменяются
  целиком**, поэтому поля, оставленные «— не задано —», сбрасываются в «не
  настроено». `created_at` профиля сохраняется, `updated_at` обновляется.
* После сохранения всплывает сообщение `Профиль **user_id** обновлён · <summary>
  · применён к агентам: N` — живые агенты этого `user_id` получили новые
  настройки немедленно.
* Справа под формой — предпросмотр **«Что уходит в системный промпт (блок
  профиля)»**: готовый текст блока считается локально, без обращения к API. Если
  все поля пусты, вместо него подпись «Профиль пуст: в системный промпт ничего не
  добавляется, агент отвечает как обычно.»
* Кнопка **«🗑 Удалить профиль»** (`DELETE /users/{user_id}/profile`) удаляет
  профиль; агенты пользователя остаются и продолжают работать, но без
  персонализации (внешнего ключа между таблицами нет — §4.1).
* Ошибки валидации: неизвестное значение перечисления, неизвестное поле объекта
  или выход за границы → `422` (§14).

### 6.4 Как добавить кастомную инструкцию

Поле **«Произвольные инструкции (по одной в строке)»** — то, чем профиль
настраивается под конкретную работу. Примеры (все они попадут в промпт
дословно):

* `Обращайся ко мне по имени`
* `Всегда предлагай два варианта решения`
* `При запросе «напиши фичу» следуй порядку: аналитик → разработчик → тестировщик`
* `Объясняй простыми словами, используй аналогии`

Правила:

* **одна инструкция — одна строка**; маркеры списка «`-`», «`*`», «`•`» и
  нумерация вида «`1.`» / «`1)`» допустимы — они отбрасываются при сохранении;
  пустые строки игнорируются, дубли схлопываются;
* до **30 инструкций**, каждая до **500 символов**, вся строка целиком — до
  4000 символов; за границами — `422`;
* каждая инструкция становится отдельной строкой блока «Дополнительные
  инструкции пользователя (выполняй буквально):» с маркером «-» и уходит в
  **каждый** запрос;
* placeholder поля показывает пример: «При запросе «напиши фичу» следуй порядку:
  аналитик → разработчик → тестировщик».

Отличие от настроек стиля: `tone`/`verbosity`/`format` описывают, **как** отвечать,
а произвольные инструкции — **что делать** (порядок ролей, число вариантов,
обязательные шаги). Проверить итог можно предпросмотром блока промпта (§6.3) и
expander-ом «Итоговый системный промпт» (§6.5).

### 6.5 Применить профиль к агенту

**При создании агента.** В сайдбаре, в форме **«➕ Новый агент»**, есть селектор
**«Профиль пользователя (user_id)»** — чей профиль подключать к системным
промптам агента. В списке вариантов первым всегда идёт `default` плюс все
созданные профили (§6.2). Выбранный `user_id` уходит в тело `POST /agents`.

**Переключение живого агента.** В разделе «👤 Профиль пользователя» есть блок
**«🔀 Профиль активного агента»** (быстрое переключение): подпись сообщает, какой
профиль использует активный агент («Агент **имя** (`agent_id`) использует профиль
`user_id`. Переключите пользователя — и следующий запрос уйдёт с другим системным
промптом.»), ниже — селектор **«Профиль для агента»** и кнопка **«Применить»**
(`PATCH /agents/{agent_id}` с `user_id`).

**Перезапуск не нужен:** агент перечитывает профиль, и **следующий же** запрос
уходит с новым системным промптом. Диалог, активная задача и слои памяти при
переключении не меняются — персонализация к ним не привязана (§4.4).

Тут же показано, что именно применено:

* таблица **«элемент / значение»** — по строкам применённого профиля (обращение,
  стиль, формат, длина, ограничения, инструкции);
* expander **«Итоговый системный промпт (без блоков памяти задачи)»** — как
  выглядит системное сообщение агента вместе с профилем (без рабочей и
  долговременной памяти текущего запроса). Источник — `GET
  /agents/{agent_id}/profile`;
* если у профиля нет заполненных полей, вместо таблицы подпись «У профиля нет
  заполненных полей: персонализация не применяется.»

### 6.6 Быстрое сравнение двух профилей (для видео)

Панель **«📊 Сравнение двух профилей на одном вопросе»** внизу раздела профиля
делает то же, что делает отчёт (§10), но в интерфейсе: создаются **два временных
агента** (по одному на профиль), им задаётся **один и тот же** вопрос, после
прогона агенты **удаляются**. Так разница ответов объясняется только профилем, а
не историей диалога.

1. Нужно минимум два профиля — иначе панель пишет: «Нужно минимум два профиля:
   нажмите «Строгий технический» и «Дружелюбный наставник» выше.» (§6.2).
2. Выберите **«Профиль A»** и **«Профиль B»** (в подписи — `user_id · summary`).
   По умолчанию подставляются два готовых профиля с противоположными
   настройками.
3. Поле **«Вопрос обоим профилям»** уже заполнено демонстрационным вопросом
   (`DEMO_QUESTION` = «Как ускорить медленный SQL-запрос в PostgreSQL?»).
4. Нажмите **«🚀 Спросить оба профиля»** — идут два запроса к DeepSeek, это
   занимает несколько секунд. Пустой вопрос и одинаковые профили отклоняются
   предупреждениями.

Результат — два столбца рядом:

* `<user_id>` и краткое описание настроек;
* токены и время ответа;
* сам ответ;
* **«Что повлияло на ответ»** — таблица «элемент / значение» из `elements`
  применённого профиля (только те поля, которые реально дали строку промпта);
* expander **«Системный промпт запроса»** — системное сообщение того запроса.

Если один профиль не ответил (например, `502` без ключа), ошибка показывается в
его столбце, а второй ответ остаётся на экране.

Режим «**один агент — переключили профиль — тот же вопрос**»: задайте вопрос
агенту в разделе «💬 Чат и память», затем вернитесь в профиль, переключите
профиль живого агента (§6.5) и задайте **тот же** вопрос ещё раз. В диалоге
видны оба ответа (история не сбрасывается), а плашка под полем показывает уже
другой профиль — наглядное доказательство, что персонализация меняется на лету.

### 6.7 Что видно в чате

* **Плашка после ответа** в разделе «💬 Чат и память» содержит строку вида
  `👤 профиль strict_tech: 6 элементов` — `user_id` и число элементов профиля,
  реально попавших в промпт. Если профиль не выбран, пуст или `default` без
  настроек — `👤 профиль: без персонализации`.
* **Карточка агента** над панелями слоёв содержит строку «Профиль пользователя
  (профиль дня 12, наследовано): `user_id`» — из неё всегда видно, чей профиль
  подключён к агенту. В списке агентов в сайдбаре профиль не выводится
  (там `имя · модель · сообщения · стратегия · задача`) — его смотрят в карточке
  агента или в блоке «🔀 Профиль активного агента» (§6.5).
* **Expander «Итоговый системный промпт (без блоков памяти задачи)»** в разделе
  профиля (§6.5) читает `GET /agents/{agent_id}/profile` и показывает
  `system_prompt` — то, что уйдёт в запрос вместе с блоками памяти, — а таблица
  «элемент / значение» и поле `prompt_block` объясняют, из чего он собран.

### 6.8 Персонализация через API (curl.exe и requests)

Все примеры — с бэкендом из §3 (`http://127.0.0.1:8000`); форматы тел и коды
ошибок совпадают с интерфейсом. Тело профиля описывает его **целиком**
(`extra="forbid"`: неизвестное поле → `422`).

Создать профиль (`POST`, ответ `201`, занятый `user_id` → `409`):

```powershell
curl.exe -X POST http://127.0.0.1:8000/users/ivan/profile ^
  -H "Content-Type: application/json" ^
  -d "{\"name\":\"Иван\",\"preferences\":{\"tone\":\"дружелюбный\",\"verbosity\":\"подробно\",\"language\":\"русский\",\"format\":\"markdown\"},\"constraints\":{\"max_response_length\":1200,\"forbidden_topics\":[\"политика\"],\"required_disclaimers\":[\"Это оценка, а не гарантия\"]},\"custom_instructions\":\"Обращайся ко мне по имени\nВсегда предлагай два варианта решения\"}"
```

Заменить настройки (`PUT`, профиля нет → `404`, в ответе — `applied_to_agents`):

```powershell
curl.exe -X PUT http://127.0.0.1:8000/users/ivan/profile ^
  -H "Content-Type: application/json" ^
  -d "{\"name\":\"Иван\",\"preferences\":{\"tone\":\"технический\",\"verbosity\":\"кратко\"},\"constraints\":{\"max_response_length\":600},\"custom_instructions\":\"Всегда предлагай два варианта решения\"}"
```

Прочитать профиль (`GET`, нет профиля → `404`), список всех профилей и профиль,
применённый к агенту:

```powershell
curl.exe http://127.0.0.1:8000/users
curl.exe http://127.0.0.1:8000/users/ivan/profile
curl.exe http://127.0.0.1:8000/agents/<agent_id>/profile
```

Удалить профиль (`DELETE`, ответ `{"status": "deleted", "user_id": ...}`; агенты
остаются без персонализации):

```powershell
curl.exe -X DELETE http://127.0.0.1:8000/users/ivan/profile
```

Переключить профиль **живого** агента (`PATCH`; `user_id` — часть общего
частичного обновления агента, рядом с `strategy`, `window_size` и т. п.):

```powershell
curl.exe -X PATCH http://127.0.0.1:8000/agents/<agent_id> ^
  -H "Content-Type: application/json" -d "{\"user_id\":\"friendly_mentor\"}"
```

То же на Python (`requests`), с проверкой, что профиль реально попал в запрос:

```python
import requests

BASE = "http://127.0.0.1:8000"

profile = {
    "name": "Иван",
    "preferences": {"tone": "технический", "verbosity": "кратко",
                    "language": "русский", "format": "plain text"},
    "constraints": {"max_response_length": 600},
    "custom_instructions": "Всегда предлагай два варианта решения",
}
created = requests.post(f"{BASE}/users/ivan/profile", json=profile, timeout=30)
print(created.status_code, created.json()["summary"])      # 201 · описание профиля

agent = requests.post(f"{BASE}/agents",
                      json={"name": "Иван-агент", "user_id": "ivan"},
                      timeout=30).json()
applied = requests.get(f"{BASE}/agents/{agent['agent_id']}/profile",
                       timeout=30).json()
print(applied["personalized"], len(applied["elements"]))
print(applied["prompt_block"])                             # блок персонализации
record = requests.post(f"{BASE}/agents/{agent['agent_id']}/generate",
                       json={"prompt": "Как ускорить медленный SQL-запрос?"},
                       timeout=90).json()
print(record.get("system_prompt"))                         # промпт запроса
```

Коды ответов: `201` — профиль создан, `200` — чтение/обновление/удаление,
`404` — нет профиля или агента, `409` — профиль с таким `user_id` уже есть,
`422` — невалидные поля (значение перечисления, границы, неизвестное поле),
`502` — сбой генерации у DeepSeek (поля `profile` и `system_prompt` в ответе при
этом заполнены — профиль считается до вызова модели).

---

## 7. Состояние задачи: управление

Задача дня 13 — держать **состояние задачи** явно и в одном месте, чтобы агент в
любой момент знал, где он находится, и после перезапуска продолжал с того же
места. Состояние хранится в SQLite (таблицы `task_states` и `task_transitions`),
читается из БД на каждый запрос и уходит **последним** блоком системного
сообщения.

Управлять можно тремя способами, и делают они одно и то же: кнопками в разделе
**«🧭 Состояние задачи»** (§7.3–§7.7), девятью эндпоинтами `/agents/{id}/tasks` и
`/tasks/{id}/...` (тела и коды — в [api.md](api.md)) или **репликами в чате**
(§7.8). Ожидаемое действие — то, чего система ждёт от пользователя на текущем
шаге; журнал переходов — таблица `task_transitions`, по строке на переход
(§7.1).

### 7.1 Как это устроено

**Этап** — крупная фаза задачи, **шаг** — конкретное действие внутри этапа. Этап и
шаг хранятся парой (`stage`/`current_step`), рядом лежит **ожидаемое действие** —
чего система ждёт от пользователя на этом шаге.

| Этап | Шаги | Что означает |
|---|---|---|
| `planning` | `gather_requirements`, `define_scope`, `create_plan` | собираем требования, определяем границы, утверждаем план |
| `execution` | `implement`, `test_locally` | пишем реализацию, проверяем её локально |
| `validation` | `review`, `run_tests`, `finalize` | ревью, прогон тестов, итоговое подтверждение |
| `done` | шагов нет | задача завершена; `current_step` остаётся `finalize` |
| `paused` | шагов нет | задача на паузе; сохраняется тот шаг, на котором встали |

Схема переходов:

```text
прямой ход:    planning -> execution -> validation -> done
откат:         validation -> execution;  execution -> planning
пауза:         planning|execution|validation|done -> paused
возобновление: paused -> тот же этап и шаг, с которого встали

шаги по этапам:
  planning   : gather_requirements, define_scope, create_plan
  execution  : implement, test_locally
  validation : review, run_tests, finalize
  done       : (шагов нет, current_step остаётся finalize)
  paused     : (текущий шаг сохраняется)
```

Ожидаемое действие зафиксировано для каждой пары «этап + шаг»:

| Этап/шаг | Ожидаемое действие |
|---|---|
| `planning/gather_requirements` | ожидается уточнение требований пользователем |
| `planning/define_scope` | ожидается определение границ задачи агентом |
| `planning/create_plan` | ожидается утверждение плана пользователем |
| `execution/implement` | ожидается реализация модуля |
| `execution/test_locally` | ожидается локальная проверка реализации |
| `validation/review` | ожидается ревью результата пользователем |
| `validation/run_tests` | ожидается проверка тестов |
| `validation/finalize` | ожидается итоговое подтверждение задачи пользователем |
| `paused` (любой шаг) | задача на паузе; ожидается продолжение (resume) |
| `done` | задача завершена; ожидается новая задача |

**Журнал переходов** — таблица `task_transitions`: по строке на каждый переход
(«из → в», причина, время). У строки создания задачи `from_stage`/`from_step`
пусты. Причины фиксированы: «задача создана», «следующий шаг», «пауза»,
«продолжение после паузы», «откат на предыдущий этап», «задача завершена», «откат
по реплике пользователя», «переход по запросу». Журнал видно во вкладке «📜 Журнал
переходов» раздела «🧭 Состояние задачи», в ответе `GET /tasks/{task_id}/history`
и прямо в SQLite (§7.10).

**Блок состояния в системном промпте** идёт последним — после профиля, роли
агента и блоков памяти — и выглядит дословно так:

```text
Состояние задачи (текущий этап и шаг; продолжай с этого места):
Текущий этап: execution. Текущий шаг: implement. Ожидаемое действие: ожидается реализация модуля. Предыдущие шаги: planning (gather_requirements, define_scope, create_plan) — завершены.
```

Если состояния у активной задачи нет, блока в промпте тоже нет — запрос уходит
как раньше, без ошибки. Рабочая память задачи от состояния не зависит: данные
задачи лежат в `working_memory` по тому же `task_id` (§4.1).

### 7.2 Завести задачу

Раздел **«🧭 Состояние задачи»** → форма **«Новая задача (task_id)»** (панель сама
подставляет идентификатор активной задачи агента) + селектор **«Начальный этап»**
(`planning` / `execution` / `validation`) → кнопка **«➕ Создать задачу»**. После
создания сразу видны этап, шаг и ожидаемое действие.

То же через API:

```powershell
curl.exe -X POST http://127.0.0.1:8000/agents/<agent_id>/tasks ^
  -H "Content-Type: application/json" ^
  -d "{\"task_id\":\"tz-portal\",\"initial_stage\":\"planning\"}"
```

```python
import requests

BASE = "http://127.0.0.1:8000"

state = requests.post(f"{BASE}/agents/{agent_id}/tasks",
                      json={"task_id": "tz-portal", "initial_stage": "planning"},
                      timeout=30).json()
print(state["stage"], state["current_step"])   # planning gather_requirements
print(state["prompt_block"])                   # блок для системного промпта
```

Что важно знать:

* `task_id` **уникален глобально**: повторное создание → `409` «Задача tz-portal
  уже существует». Работайте с существующей задачей или возьмите другое имя (до
  64 символов);
* создание заодно делает задачу **активной** у агента: блок состояния
  подключается к системному промпту только у активной задачи (той же, что
  переключает сайдбар, §5.5);
* начальный этап — один из `planning` / `execution` / `validation`; `paused` и
  `done` в теле дают `422` (в `paused` задача попадает паузой, в `done` —
  завершением), пустой или слишком длинный `task_id` — тоже `422`;
* если состояние не заведено, **блока состояния в системном промпте нет**: задача
  и её рабочая память работают как раньше. Создание состояния ничего не удаляет и
  рабочую память не переписывает.

Незавершённые задачи агента отдаёт `GET /agents/{agent_id}/tasks`:
приостановленные в списке остаются, завершённые уходят.

### 7.3 Следующий шаг

Кнопка **«⏭ Следующий шаг»** или

```powershell
curl.exe -X POST http://127.0.0.1:8000/tasks/tz-portal/advance
```

* шаг меняется на следующий в текущем этапе: `gather_requirements → define_scope →
  create_plan`, `implement → test_locally`, `review → run_tests → finalize`;
* с **последнего** шага этапа задача переходит на первый шаг следующего этапа:
  `planning/create_plan` → `execution/implement`, `execution/test_locally` →
  `validation/review`, `validation/finalize` → `done/finalize`;
* ожидаемое действие пересчитывается на новый шаг, причина в журнале —
  «следующий шаг»;
* кнопка заблокирована, когда задача завершена или на паузе; те же случаи по API —
  `400` («задача завершена: этап done терминальный», «задача уже на паузе»),
  сначала нужно «▶️ Продолжить»;
* в `done` этап терминальный — дальше шага нет, `current_step` остаётся `finalize`.

### 7.4 Пауза

Кнопка **«⏸ Пауза»** или

```powershell
curl.exe -X POST http://127.0.0.1:8000/tasks/tz-portal/pause
```

* этап и шаг **сохраняются**: задача встаёт в `paused`, а `context.paused_from_stage`
  и `context.paused_from_step` помнят, откуда она встала (в ответе видно
  `paused_from_stage`, в панели — строку «⏸ Пауза с этапа execution»);
* ожидаемое действие становится «задача на паузе; ожидается продолжение (resume)»,
  причина в журнале — «пауза»;
* пауза возможна из **любого** этапа, включая `done`; повторная пауза → `400`
  «задача уже на паузе», на паузе кнопка заблокирована;
* пауза — то, что позволяет прервать работу и вернуться к ней позже: ничего не
  теряется, ни этап, ни шаг (§7.9).

### 7.5 Продолжение

Кнопка **«▶️ Продолжить»** или

```powershell
curl.exe -X POST http://127.0.0.1:8000/tasks/tz-portal/resume
```

* задача возвращается на **сохранённый** этап и шаг (`paused/create_plan` →
  `planning/create_plan`), причина — «продолжение после паузы»;
* ожидаемое действие снова то, что соответствует этому шагу;
* кнопка активна только на паузе; `resume` у задачи не на паузе → `400` «задача не
  на паузе»;
* это ровно то, что делает реплика «продолжи» в чате (§7.8), и то, чем
  заканчивается сценарий с перезапуском бэкенда (§7.9).

### 7.6 Откат на предыдущий этап

Кнопка **«↩️ Откат на предыдущий этап»** или

```powershell
curl.exe -X POST http://127.0.0.1:8000/tasks/tz-portal/rollback ^
  -H "Content-Type: application/json" -d "{\"to_stage\":\"execution\"}"
```

* откат идёт на **один** этап назад: `validation → execution`,
  `execution → planning`; шаг сбрасывается на первый шаг целевого этапа
  (`execution/implement`, `planning/gather_requirements`);
* `to_stage` обязан **совпасть** с целью отката: `{"to_stage": "validation"}` из
  `execution` → `400` «откат с этапа execution возможен только на этап planning».
  Готовая цель лежит в поле `rollback_stage` ответа — по ней панель включает и
  выключает кнопку, поэтому ошибиться в интерфейсе нечем;
* откат не описан из `planning` (предыдущего этапа нет), из `done`
  (терминальный) и на паузе — все три случая дают `400`; кнопка в этих состояниях
  заблокирована;
* причина в журнале — «откат на предыдущий этап» (или «откат по реплике
  пользователя», если инициатива пришла из чата);
* рабочая память задачи откатом **не** трогается: сбрасывается положение в
  процессе, а не данные.

### 7.7 Завершение задачи

Кнопка **«✅ Завершить задачу»** — это один вызов:

```powershell
curl.exe -X POST http://127.0.0.1:8000/tasks/tz-portal/transition ^
  -H "Content-Type: application/json" ^
  -d "{\"stage\":\"done\",\"reason\":\"задача завершена\"}"
```

* разрешён переход `validation → done`: шаг становится `finalize`, `is_active` —
  `false`, ожидаемое действие — «задача завершена; ожидается новая задача»;
* `step`, `expected_action` и `reason` в теле необязательны: умолчания (`finalize`,
  ожидаемое действие шага, «переход по запросу») разрешает менеджер, поэтому
  короткого тела `{"stage": "done"}` достаточно;
* после `done` не работают ни «Следующий шаг», ни откат (`400` — задача
  терминальная); доступны только пауза и чтение;
* завершённая задача уходит из `GET /agents/{agent_id}/tasks` и репликами не
  воскрешается (§7.8);
* кнопка заблокирована, если задача уже завершена.

### 7.8 Реплики вместо кнопок

`Agent.apply_task_intent(prompt)` вызывается **до** сборки контекста, поэтому блок
состояния в системном промпте того же запроса уже описывает новое положение.
Фразы ищутся на границе слова, приоритет групп фиксирован: **пауза → продолжение →
откат → подтверждение**.

| Намерение | Фразы | Что делает |
|---|---|---|
| `pause` | «пауза», «паузу», «поставь на паузу», «поставь задачу на паузу», «останови задачу», «приостанови» | задача встаёт в `paused`, шаг сохраняется (§7.4) |
| `resume` | «продолжи», «продолжить», «продолжаем», «возобнови», «с того же места» | возврат на сохранённый этап и шаг (§7.5) |
| `rollback` | «откат», «откатись», «вернись на предыдущий», «вернись к предыдущему» | откат на один этап назад, шаг — первый шаг этапа (§7.6) |
| `advance` | «подтверждаю», «подтверждаю план», «шаг выполнен», «выполнено», «готово», «следующий шаг», «дальше» | следующий шаг, а с последнего шага — следующий этап (§7.3) |

Правила:

* «подтверждаю, продолжаем» — это `resume`: продолжение приоритетнее
  подтверждения;
* совпадение — по границе слова: «продолжительность сессии» намерением **не**
  считается, а «продолжи» — считается. Обычная реплика состояние не меняет;
* реплика двигает только **активную** задачу агента;
* недопустимый для текущего этапа переход диалог **не роняет**: он пишется в лог,
  а запрос выполняется как обычно — на `planning` реплика «вернись на предыдущий
  этап» ничего не меняет;
* у завершённой задачи реплики состояние не меняют: из `done` задача не
  воскрешается, нужна новая задача;
* в ответе `POST /agents/{agent_id}/generate` есть поле `task_state` (текущее
  состояние или `null`), а `system_prompt` содержит блок состояния — оба
  заполняются **до** вызова DeepSeek, поэтому видны и при `502`.

### 7.9 Сценарий: продолжить после перезапуска без повторных объяснений

Главная проверка дня: состояние живёт в SQLite, а не в памяти процесса.

1. Заведите задачу `tz-portal` (§7.2) и дойдите до `execution/implement` —
   «⏭ Следующий шаг» трижды из `planning/gather_requirements`.
2. Нажмите «⏸ Пауза»: в панели `paused/implement`, строка «⏸ Пауза с этапа
   execution», в журнале — причина «пауза».
3. Остановите бэкенд (Ctrl+C в первом терминале, §3) и запустите его снова тем же
   приказом — на **той же** базе `day13/agents.db`:

   ```powershell
   cd day13
   uv run uvicorn backend.api.main:app --port 8000
   ```

4. Убедитесь, что состояние на месте:

   ```powershell
   curl.exe http://127.0.0.1:8000/tasks/tz-portal/state
   ```

   В ответе `"stage": "paused"`, `"current_step": "implement"`,
   `"paused_from_stage": "execution"`: процесс новый, а состояние то же.

5. В чате напишите одну реплику — `продолжи`. Задача возвращается на
   `execution/implement`, и блок в системном промпте этого же запроса уже
   описывает новое положение: агенту не нужно объяснять, где он остановился
   (§7.5, §7.8).

Доказательство прогоном — [`reports/task_state_demo.md`](reports/task_state_demo.md): пять фаз,
каждая в **отдельном процессе** (`subprocess`), с реальными ответами DeepSeek.
Фаза 1 оставляет задачу в `paused/create_plan`, фаза 2 в новом процессе читает это
состояние и репликой «продолжи» возвращает задачу на тот же шаг, фазы 3–4 доводят
её до `done`, фаза 5 читает итог и печатает журнал (15 переходов).

```powershell
cd day13
uv run python scripts/task_state_demo.py --reset         # очистить демо-БД и отчёт
uv run python scripts/task_state_demo.py --phase 2       # одна фаза в отдельном процессе
uv run python scripts/task_state_demo.py --all           # все 5 фаз, нужен ключ DeepSeek
uv run python scripts/task_state_demo.py --all --no-api  # то же офлайн, без сети
```

Скрипт работает на **отдельной** базе `day13/task_state_demo.db`, агентом `demo13`
и задачей `tz-portal`; ваша `agents.db` не затрагивается. Текст отчёта собирает
`scripts/task_demo_report.py` (как `scripts/comparison_report.py` у дня 12), офлайн-заглушку даёт
`scripts/comparison_stub.py`.

### 7.10 Проверить в SQLite

Состояние и журнал — обычные строки в `day13/agents.db` (команды выполняются из
папки `day13`):

```powershell
uv run python -c "import sqlite3; print(sqlite3.connect('agents.db').execute('select id, task_id, agent_id, stage, current_step, expected_action, updated_at from task_states order by id').fetchall())"
uv run python -c "import sqlite3; print(sqlite3.connect('agents.db').execute('select id, task_id, from_stage, from_step, to_stage, to_step, reason, created_at from task_transitions order by id').fetchall())"
uv run python -c "import sqlite3; print(sqlite3.connect('agents.db').execute('select task_id, substr(context,1,120), substr(history,1,80) from task_states order by id').fetchall())"
```

Первая выборка — по строке на задачу (этап, шаг, ожидаемое действие), вторая —
весь журнал переходов (`from_stage`/`from_step` пусты у создания), третья — снимок
контекста (в нём `working_memory` и метка паузы `paused_from_stage`/
`paused_from_step`) и история внутри строки состояния. Полный разбор остальных
таблиц — в §14.

---

## 8. Проверка 1: какие данные попадают в каждый слой

Сеть и ключ не нужны — работают только слои памяти.

1. Создайте агента (§5.1) и задачу `tz-portal`. Откройте вкладку «🗂 Рабочая» и
   сохраните две записи: `цель` → `Корпоративный портал`,
   `ограничение` → `только on-premise`.
2. Перейдите на вкладку «🧠 Долговременная» и сохраните две записи:
   `preference / язык_интерфейса: русский` (уверенность 1.0) и
   `profile / роль_пользователя: аналитик` (0.9).
3. Отправьте четыре реплики — по одной, дожидаясь ответа: поле «Сообщение
   агенту», кнопка «🚀 Отправить» (§5.7). Дословные тексты и ожидаемые числа:

| Ход | Что ввести дословно | 👤 Краткосрочная | 🗂 Рабочая | 🧠 Долговременная |
|---|---|---|---|---|
| 1 | `Стек: Python 3.14 + FastAPI` | `0 записей · 0 токенов` | `2 записи` | `2 записи` |
| 2 | `База данных: PostgreSQL` | `2 записи` | `2 записи` | `2 записи` |
| 3 | `Срок: 3 месяца` | `4 записи` | `2 записи` | `2 записи` |
| 4 | `Интеграции: отправка почты через SMTP` | `6 записей` | `2 записи` | `2 записи` |

Почему так: в краткосрочный слой уходит то, что отправила стратегия (при
`summary` — последние `keep_last_messages = 6` реплик, отсюда `0 → 2 → 4 → 6`),
а рабочая и долговременная не меняются: ход диалога не пишет в эти таблицы.
Токены каждого слоя больше нуля, но зависят от текста — сверяйте **число
записей**; `total_tokens` в подписи индикатора равен сумме трёх слоёв.

| Шаг | Слой | Что появилось |
|---|---|---|
| 1 (формы) | рабочая | `working_memory`: `цель`, `ограничение` задачи `tz-portal` |
| 2 (формы) | долговременная | `long_term_memory`: `preference/язык_интерфейса`, `profile/роль_пользователя` |
| 3 (чат) | краткосрочная | `short_term_messages`: пары `user` + `assistant` текущей сессии |
| 3 (чат) | рабочая, долговременная | **ничего**: ход диалога не создаёт записей в этих таблицах |
| 4 (индикатор) | все три | записи и токены каждого слоя в `record["memory"]` |

Проверка в БД (однострочники выполняются из папки `day13`):

```powershell
uv run python -c "import sqlite3; print(sqlite3.connect('agents.db').execute('select id, agent_id, session_id, role, substr(content,1,60), created_at from short_term_messages order by id').fetchall())"
uv run python -c "import sqlite3; print(sqlite3.connect('agents.db').execute('select agent_id, task_id, key, value, updated_at from working_memory order by task_id, key').fetchall())"
uv run python -c "import sqlite3; print(sqlite3.connect('agents.db').execute('select agent_id, category, key, value, confidence from long_term_memory order by category, key').fetchall())"
```

Детерминированная версия той же проверки — без сети, ключа и UI: наследованные
тесты слоёв (`tests/integration/test_memory_agent.py`, `tests/integration/test_memory_manager.py`,
`tests/e2e/test_memory_api.py`) проверяют маршрутизацию «действие → слой», очистку
`new_session()` и переключение задачи. Команда запуска — §10.

---

## 9. Проверка 2: как слои влияют на ответы

Здесь нужен ключ DeepSeek (§2) — сравнение идёт по смыслу ответа. Числа из
`record["memory"]` детерминированы и проверяются всегда, формулировка модели
свободная: сверяйте смысл, а не строку.

### 9.1 Долговременная память

Начинайте на свежем агенте, чтобы числа слоёв были однозначны.

| Шаг | Действие | Что должно быть в индикаторе | Что в ответе |
|---|---|---|---|
| а | задать вопрос «На каком языке должен быть интерфейс портала?» | «🧠 Долговременная» — `0 записей · 0 токенов` | модель ничего не знает о предпочтении (уточняет или отвечает обобщённо) |
| б | сохранить `preference / язык_интерфейса: русский` и задать тот же вопрос | «🧠 Долговременная» — `1 запись` (и токены > 0) | ответ учитывает русский язык интерфейса |
| в | нажать «🆕 Новая сессия» и задать тот же вопрос | краткосрочная `0` токенов, долговременная — по-прежнему `1 запись` | предпочтение снова учтено, хотя диалог пуст |

Примеры фактических ответов (формулировка свободная, сверяйте смысл): (а) общий
ответ про аудиторию портала, без упоминания русского; (б) «Судя по вашему
профилю — язык интерфейса: **русский**»; (в) «Интерфейс портала должен быть на
русском языке.»

Шаг «в» — главное доказательство: сессии сменились, диалога нет, а ответ
по-прежнему опирается на долговременный слой.

### 9.2 Рабочая память

| Шаг | Действие | Что должно быть в индикаторе | Что в ответе |
|---|---|---|---|
| а | в задаче `tz-portal` сохранить `ограничение: только on-premise`, спросить «Можно ли разместить в облаке?» | «🗂 Рабочая» — `1 запись` | ответ учитывает ограничение (облако не подходит: «у вас зафиксировано ограничение — только on-premise») |
| б | нажать «🆕 Новая сессия», переключиться на задачу `tz-cloud` (§5.5) и задать тот же вопрос | «🗂 Рабочая» — `0 записей · 0 токенов`, краткосрочная — `0` токенов | ограничение не упоминается — оно принадлежит другой задаче, а диалог пуст |
| в | нажать «🆕 Новая сессия», вернуться на `tz-portal` и задать тот же вопрос | «🗂 Рабочая» — `1 запись` | ограничение снова в ответе |

Примеры фактических ответов: (а) «Нет, нельзя. Есть ограничение: только
on-premise.»; (б) общий ответ про размещение в облаке, ограничение не
упомянуто; (в) «Нет. У вас зафиксировано ограничение — только on-premise.»

Так видно, что рабочая память привязана к `task_id`, а не к диалогу: сам диалог
при переключении задачи не теряется.

> [i] Шаги «🆕 Новая сессия» в (б) и (в) обязательны. В той же сессии
> предыдущий ответ про on-premise остаётся в **краткосрочном** слое, и модель
> цитирует ограничение из диалога — тогда опыт доказывает работу диалога, а не
> рабочей памяти. С пустой сессией единственный источник ограничения — записи
> задачи, и разница между `tz-portal` и `tz-cloud` видна однозначно.

### 9.3 Профиль: ответ меняется без изменения памяти

Профиль работает поверх слоёв и не зависит от них (§4.4). На свежем агенте с
профилем `default` (без настроек):

| Шаг | Действие | Что должно быть в ответе |
|---|---|---|
| а | задать вопрос «Как ускорить медленный SQL-запрос в PostgreSQL?» | ответ в обычном стиле агента; плашка `👤 профиль: без персонализации` |
| б | применить к живому агенту профиль `strict_tech` (§6.5) и задать тот же вопрос | сухой ответ без разметки, укладывающийся в предел 600 символов; плашка `👤 профиль strict_tech: N элементов` |
| в | применить профиль `friendly_mentor` и задать тот же вопрос | тёплый подробный ответ с markdown-списками; диалог не сброшен — видны все три ответа |
| г | нажать «🆕 Новая сессия» и задать тот же вопрос | профиль тот же (`friendly_mentor`): персонализация сохраняется вместе с профилем, а не с диалогом |

Формулировки свободные, строгий предел длины модель соблюдает приблизительно —
сверяйте стиль, формат и наличие ограничения, а не точное число символов. Что
проверяется жёстко — что блок профиля **попал** в промпт: это видно в
`record["profile"]["elements"]` и в expander-е «Итоговый системный промпт»
(§6.5), а также в отчёте (§10).

---

## 10. Проверка 3: персонализация — один вопрос, два профиля

Доказательство персонализации (наследовано из дня 12) — отчёт
[`reports/personalization_comparison.md`](reports/personalization_comparison.md), который
собирает скрипт `scripts/personalization_comparison.py` из папки `day13`.

```powershell
cd day13
uv run python scripts/personalization_comparison.py            # реальные запросы к deepseek-chat
uv run python scripts/personalization_comparison.py --no-api   # офлайн-заглушка, без сети
uv run pytest -q                             # 584 теста
```

Те же команды короче, если `uv run` уже не нужен (например, в активированном
окружении): `python scripts/personalization_comparison.py`,
`python scripts/personalization_comparison.py --no-api`,
`cd day13 && pytest -q`. Базовый вариант — через `uv run` выше: он не зависит от
того, какое окружение активно в терминале.

* первый запуск требует `DEEPSEEK_API_KEY` в `day13/.env` (§2) — в отчёте
  реальные ответы модели;
* `--no-api` — офлайн-прогон на заглушке: сеть и ключ не нужны, подстановка
  профиля в промпт проверяется без вызовов DeepSeek;
* скрипт пишет `day13/docs/reports/personalization_comparison.md` и работает на **отдельной
  БД** `day13/personalization_demo.db` (пересоздаётся при каждом прогоне), ваша
  `day13/agents.db` не затрагивается.

Что внутри отчёта:

* таблица «**Профиль | Настройки | Ответ агента | Какие элементы профиля
  повлияли**» — два противоположных профиля и их ответы на один вопрос
  (`DEMO_QUESTION` = «Как ускорить медленный SQL-запрос в PostgreSQL?»);
* полные ответы вместе с системными промптами запросов — видно, что блок
  профиля стоит в промпте;
* раздел про профиль с инструкцией о порядке ролей (вопрос
  `DEMO_FEATURE_REQUEST` = «напиши фичу: экспорт отчётов в Excel»);
* таблица наблюдений: ограничение длины, отсутствие markdown у `plain text`,
  наличие markdown у `markdown`, длина подробного ответа против краткого,
  порядок ролей аналитик → разработчик → тестировщик;
* вывод.

Обязательные проверки (не зависят от капризов модели): подстановка профиля в
системный промпт и следование инструкции о порядке ролей. Проверки стиля и
формата — наблюдения: модель соблюдает ограничение длины приблизительно.

### Тесты

`cd day13 && uv run pytest -q` — **584 теста**, все зелёные: 235
наследованных из дня 11 (слои памяти, стратегии, сжатие, API памяти), 79 по
персонализации дня 12 и 270 новых по состоянию задачи.

| Файл | Тестов | Что проверяет |
|---|---|---|
| `tests/unit/test_task_fsm.py` | 86 | чистые правила `backend/domain/task_fsm.py`: таблица переходов (все 20 пар «этап × событие»), шаги этапов, `is_valid_transition`, `rollback_target`, явные ошибки `UnknownTaskEvent`/`InvalidTaskTransition` |
| `tests/unit/test_task_prompt.py` | 26 | тексты блока состояния: ожидаемое действие для каждого шага, перечень завершённых этапов, дословный формат строки промпта |
| `tests/unit/test_task_intent.py` | 63 | распознавание намерения по реплике: фразы всех четырёх групп, приоритет пауза → продолжение → откат → подтверждение, граница слова («продолжительность» не намерение) |
| `tests/integration/test_task_state.py`, `tests/integration/test_task_store.py` | 47 | таблицы `task_states`/`task_transitions`: создание, цепочка шагов, пауза и продолжение, откат, завершение, журнал, снимок рабочей памяти в `context`, коды ошибок хранилища |
| `tests/integration/test_task_manager.py` | 13 | обёртки `TaskOpsMixin`: умолчания шага и ожидаемого действия, `list_active_tasks`, причины переходов |
| `tests/integration/test_task_agent.py` | 10 | блок состояния в системном сообщении, поля `task_state` в отчёте генерации, авто-обновление по реплике, чтение состояния новым агентом (перезапуск) |
| `tests/e2e/test_task_api.py` | 25 | девять эндпоинтов `/tasks...`: `201`/`400`/`404`/`409`/`422`, полный цикл, журнал, поле `task_state` в генерации |

Быстрая синтаксическая проверка изменённых файлов (после рефакторинга — вместе с
роутерами и пакетом `frontend/`):
`uv run python -m py_compile backend/api/main.py backend/core/dependencies.py backend/api/agents.py backend/api/context.py backend/api/memory.py backend/api/profiles.py backend/api/tasks.py backend/domain/task_fsm.py backend/domain/task_prompt.py backend/domain/task_intent.py backend/storage/task_store.py backend/services/task_state.py backend/models/task_state.py backend/agents/manager_tasks.py backend/schemas/task.py frontend/api_client.py frontend/chat_section.py frontend/sidebar.py frontend/task_panel.py app.py`.

---

## 11. Проверка 4: состояние задачи — цикл, ошибки и перезапуск

Ключ DeepSeek для шагов 1–4 не нужен: проверяются хранилище, переходы и коды
ошибок. Порядок — как в сквозном прогоне дня 13.

**1. Полный цикл через API.** Бэкенд из §3; `agent_id` берётся из ответа
`POST /agents`:

```python
import requests

BASE = "http://127.0.0.1:8000"

agent_id = requests.post(f"{BASE}/agents", json={"name": "Проверка FSM"},
                         timeout=30).json()["agent_id"]

state = requests.post(f"{BASE}/agents/{agent_id}/tasks",
                      json={"task_id": "tz-portal", "initial_stage": "planning"},
                      timeout=30).json()
print(state["stage"], state["current_step"])          # planning gather_requirements

for _ in range(3):                                    # шаг вперёд трижды
    state = requests.post(f"{BASE}/tasks/tz-portal/advance", timeout=30).json()
print(state["stage"], state["current_step"])          # execution implement

state = requests.post(f"{BASE}/tasks/tz-portal/pause", timeout=30).json()
print(state["stage"], state["current_step"])          # paused implement

state = requests.post(f"{BASE}/tasks/tz-portal/resume", timeout=30).json()
print(state["stage"], state["current_step"])          # execution implement

for _ in range(2):
    state = requests.post(f"{BASE}/tasks/tz-portal/advance", timeout=30).json()
print(state["stage"], state["current_step"])          # validation run_tests

state = requests.post(f"{BASE}/tasks/tz-portal/rollback",
                      json={"to_stage": "execution"}, timeout=30).json()
print(state["stage"], state["current_step"])          # execution implement

state = requests.post(f"{BASE}/tasks/tz-portal/transition",
                      json={"stage": "done", "reason": "задача завершена"},
                      timeout=30).json()
print(state["stage"], state["current_step"], state["is_active"])  # done finalize False
```

**2. Коды ошибок** (те же вызовы, другой ответ):

| Запрос | Ответ |
|---|---|
| `POST /tasks/tz-portal/advance` после `done` | `400` «задача завершена: этап done терминальный» |
| `POST /tasks/tz-portal/rollback` с `to_stage`, не равным цели отката | `400` «откат с этапа execution возможен только на этап planning» |
| `POST /tasks/tz-portal/rollback` из `planning` | `400` «у этапа planning нет предыдущего этапа» |
| `POST /tasks/tz-portal/pause` дважды | `400` «задача уже на паузе»; `resume` не на паузе — `400` «задача не на паузе» |
| `GET /tasks/nope/state` (и `/history`, `pause`, `resume`, `advance`) | `404` «Задача nope не найдена» |
| `POST /agents/{agent_id}/tasks` повторно с тем же `task_id` | `409` «Задача tz-portal уже существует» |

**3. Журнал.** `GET /tasks/tz-portal/history` отдаёт `entries` по возрастанию
`id`, включая строку создания (`from_stage`/`from_step` пусты). В сквозном прогоне
дня 13 журнал собрал **14 записей** с причинами «задача создана», «следующий шаг»,
«пауза», «продолжение после паузы», «откат на предыдущий этап», «задача
завершена»; в демонстрации `reports/task_state_demo.md` — **15** (там на один откат и
возврат больше).

**4. Перезапуск процесса.** Остановите uvicorn (Ctrl+C) и запустите снова тем же
приказом на **той же** базе `day13/agents.db`:

```powershell
curl.exe http://127.0.0.1:8000/tasks/tz-portal/state
```

Ответ содержит тот же `"stage"` и `"current_step"` (в прогоне дня 13 —
`execution/implement`) и то же `expected_action`: состояние прочитано из SQLite,
память процесса ни при чём.

**5. Интерфейс.** Раздел «🧭 Состояние задачи» при запущенном бэкенде
(`streamlit run app.py`, §3): «⏭ Следующий шаг» переводит `implement` →
`test_locally`, «⏸ Пауза» — в `paused/test_locally` со строкой «Пауза с этапа
execution», «▶️ Продолжить» возвращает `execution/test_locally`. Таблица журнала и
вкладка «🧩 Блок в системном промпте» обновляются тем же кликом.

**6. Автоматические проверки.** Быстрая выборка по состоянию задачи и оба
прогона демонстрации:

```powershell
cd day13
uv run pytest -q tests/unit/test_task_fsm.py tests/integration/test_task_state.py tests/e2e/test_task_api.py
uv run python scripts/task_state_demo.py --all --no-api   # офлайн, без сети
uv run python scripts/task_state_demo.py --all            # реальные ответы DeepSeek
```

Полный прогон — **584 теста** (§10). Итог виден в панели «🧭 Состояние задачи»,
в `GET /tasks/{task_id}/state` и `/history`, в таблицах `task_states` /
`task_transitions` (§7.10) и в отчёте `reports/task_state_demo.md`.

---

## 12. Сценарий демонстрации для видео (день 13)

Покадрово, 5–7 минут. Числа, цитаты ответов и полный журнал для сверки — в
[`reports/task_state_demo.md`](reports/task_state_demo.md).

**Кадр 0 — интро (30 сек).** Задание дня: вынести состояние задачи в явный
конечный автомат, чтобы агент после перезапуска продолжал с того же места.
Показать таблицу этапов и шагов (§7.1), схему переходов, переключатель разделов
(«💬 Чат и память» / «👤 Профиль пользователя» / «🧭 Состояние задачи») и таблицы
`task_states` / `task_transitions` (§7.10).

**Кадр 1 — завести задачу (30 сек).** Раздел «🧭 Состояние задачи»: форма
«Новая задача (task_id)» (`tz-portal`) + селектор «Начальный этап» → «➕ Создать
задачу». Показать `planning/gather_requirements`, ожидаемое действие и дословный
блок во вкладке «🧩 Блок в системном промпте».

**Кадр 2 — «⏭ Следующий шаг» (1 мин).** Нажать три раза:
`gather_requirements → define_scope → create_plan → execution/implement`.
Проговорить: на последнем шаге этапа меняется и этап, а во вкладке «📜 Журнал
переходов» появляются строки с причиной «следующий шаг».

**Кадр 3 — «⏸ Пауза» (30 сек).** Нажать «⏸ Пауза»: `paused/implement`, строка
«⏸ Пауза с этапа execution», причина «пауза» в журнале. Этап и шаг сохранены —
вернуться можно на них же.

**Кадр 4 — остановить и запустить бэкенд (1 мин).** Ctrl+C в первом терминале,
затем снова `uvicorn backend.api.main:app --port 8000` на той же `day13/agents.db`.
`curl.exe http://127.0.0.1:8000/tasks/tz-portal/state` → те же
`"stage": "paused"`, `"current_step": "implement"`. Главное доказательство дня:
состояние живёт в SQLite, а не в памяти процесса.

**Кадр 5 — «▶️ Продолжить» (30 сек).** Реплика `продолжи` в чате (или кнопка
«▶️ Продолжить»): задача возвращается на `execution/implement`, и блок состояния в
системном промпте этого же запроса уже описывает новое положение.

**Кадр 6 — откат (1 мин).** Дойти «⏭ Следующий шаг» до `validation/run_tests` и
нажать «↩️ Откат на предыдущий этап»: `execution/implement`, в журнале — «откат на
предыдущий этап». Показать, что кнопка заблокирована из `planning` и из `done`, а
`{"to_stage": "validation"}` возвращает `400`.

**Кадр 7 — «✅ Завершить задачу» (30 сек).** Кнопка переводит задачу в
`done/finalize` (это один вызов `POST /tasks/{task_id}/transition`), «⏭ Следующий
шаг» после этого даёт `400`, а задача уходит из `GET /agents/{id}/tasks`.

**Кадр 8 — журнал и отчёт (1 мин).** Вкладка «📜 Журнал переходов» — весь путь
задачи; затем `uv run python scripts/task_state_demo.py --all` (или `--all --no-api`) и открыть
`reports/task_state_demo.md`: пять фаз в пяти процессах, таблицы переходов, ответы агента,
блок состояния из системного промпта и вывод «состояние сохранено между пятью
запусками».

**Финал (30 сек).** Состояние задачи — не текст в промпте, а строка `task_states`
с журналом `task_transitions`: читается из БД на каждый запрос, переживает
перезапуск бэкенда, обновляется одной репликой и не путается с рабочей памятью
задачи.

---

## 13. Наследовано из дней 9–10 (кратко)

Всё ниже — возможности предыдущих дней, которые в дне 13 физически есть в
интерфейсе. Они описываются здесь ровно затем, чтобы документ соответствовал
приложению; подробности — по ссылкам.

| Что | Зачем в дне 13 | Подробности |
|---|---|---|
| Стратегия контекста: `sliding_window` / `sticky_facts` / `branching` / `summary`; сайдбар «⚙️ Стратегия контекста», `POST /agents/{id}/strategy` | это **политика сборки краткосрочного слоя**: сколько реплик сессии уходит в запрос и в каком виде; блок профиля попадает в запрос при любой стратегии | [`../../day10/docs/usage.md`](../../day10/docs/usage.md) |
| Панель «🗜 Сжатие контекста» (таблица `summaries`, `POST /summarize`, `GET /summary`) | при стратегии `summary` сжимает старую часть краткосрочного слоя в конспект | [`../../day10/docs/usage.md`](../../day10/docs/usage.md) |
| Панель «📊 Токены диалога» (`GET /usage`, `GET /usage/graph`) | расход токенов и денег; в `token_usage` лежат и токены по слоям | [`../../day10/docs/usage.md`](../../day10/docs/usage.md) |
| Блок «⚖️ Сравнить режимы» (`POST /compare`) | «полная история vs конспект» на одном промпте; без галочки работает без ключа | [`../../day10/docs/usage.md`](../../day10/docs/usage.md) |
| Панель «📌 Факты диалога» (таблица `facts`, только `sticky_facts`) | факты «ключ: значение», извлечённые эвристикой, уходят отдельным блоком | [`../../day10/docs/usage.md`](../../day10/docs/usage.md) |
| Панель «🌿 Ветвление истории» (таблица `checkpoints`, только `branching`) | ветки диалога; переключение ветки перезаписывает краткосрочный слой | [`../../day10/docs/usage.md`](../../day10/docs/usage.md) |

Ни один из этих пунктов не относится ни к персонализации, ни к состоянию задачи:
это унаследованная инфраструктура дня 10 (и дня 9 для сжатия), а слои памяти
пришли из дня 11. Профиль дня 12 и состояние задачи дня 13 — надстройки над ними:
ни одна из панелей не заменяется, маршрутизация записей по слоям не меняется.
Захардкоженного демонстрационного сценария по слоям в дне 13 нет — диалог ведётся
вручную в чате (§5.7), доказательства дают `scripts/task_state_demo.py` (§7.9) и
`scripts/personalization_comparison.py` (§10).

---

## 14. Просмотр SQLite

База — обычный файл `day13/agents.db`, смотреть можно без установки чего-либо
лишнего. Примеры выполняются из папки `day13`; порядок — по слоям, затем
персонализация, в конце — состояние задачи.

Краткосрочная память (реплики сессий; при сжатии не удаляются, при новой сессии
прошлая сессия удаляется):

```powershell
uv run python -c "import sqlite3; print(sqlite3.connect('agents.db').execute('select id, agent_id, session_id, role, substr(content,1,60), created_at from short_term_messages order by id').fetchall())"
```

Рабочая память (пары задача+ключ уникальны):

```powershell
uv run python -c "import sqlite3; print(sqlite3.connect('agents.db').execute('select agent_id, task_id, key, value, updated_at from working_memory order by task_id, key').fetchall())"
```

Долговременная память (категория + ключ уникальны):

```powershell
uv run python -c "import sqlite3; print(sqlite3.connect('agents.db').execute('select agent_id, category, key, value, confidence from long_term_memory order by category, key').fetchall())"
```

Агенты (конфиг, настройки сжатия, стратегия, окно, активные сессия/задача и
профиль пользователя):

```powershell
uv run python -c "import sqlite3; print(sqlite3.connect('agents.db').execute('select agent_id, name, user_id, strategy, window_size, current_session_id, current_task_id, summary_enabled from agents').fetchall())"
```

Профили пользователей (наследовано из дня 12; `user_id` уникален):

```powershell
uv run python -c "import sqlite3; print(sqlite3.connect('agents.db').execute('select id, user_id, name, preferences, constraints, custom_instructions, created_at, updated_at from user_profiles order by user_id').fetchall())"
```

Конспекты (append-only: текущий — последняя строка по `id`):

```powershell
uv run python -c "import sqlite3; print(sqlite3.connect('agents.db').execute('select id, agent_id, covered_from_message_id, covered_to_message_id, covered_messages, source_tokens, summary_tokens, cost from summaries order by id').fetchall())"
```

Метрики, включая поля сжатия и расход по слоям памяти:

```powershell
uv run python -c "import sqlite3; print(sqlite3.connect('agents.db').execute('select id, agent_id, total_tokens, mode, saved_tokens, short_term_tokens, working_tokens, long_term_tokens from token_usage order by id').fetchall())"
```

Факты (стратегия sticky_facts; пара agent_id+key уникальна):

```powershell
uv run python -c "import sqlite3; print(sqlite3.connect('agents.db').execute('select agent_id, key, value, updated_at from facts order by key').fetchall())"
```

Чекпоинты/ветки (стратегия branching; parent_id — дерево, messages — JSON-снимок):

```powershell
uv run python -c "import sqlite3; print(sqlite3.connect('agents.db').execute('select id, agent_id, parent_id, json_array_length(messages), created_at from checkpoints order by id').fetchall())"
```

Состояние задачи (день 13; этап, шаг и ожидаемое действие — по строке на задачу):

```powershell
uv run python -c "import sqlite3; print(sqlite3.connect('agents.db').execute('select id, task_id, agent_id, stage, current_step, expected_action, updated_at from task_states order by id').fetchall())"
```

Журнал переходов задачи (строка создания — с пустыми `from_stage`/`from_step`):

```powershell
uv run python -c "import sqlite3; print(sqlite3.connect('agents.db').execute('select id, task_id, from_stage, from_step, to_stage, to_step, reason, created_at from task_transitions order by id').fetchall())"
```

Для интерактивной работы можно открыть файл любым SQLite-клиентом — формат
стандартный. Внешнего ключа между `user_profiles` и `agents` нет: удаление
профиля агентов не уносит. Строки состояния задачи, наоборот, привязаны к агенту
(`task_states.agent_id`, `task_transitions.task_id`) и удаляются вместе с ним:
`DELETE /agents/{id}` уносит и состояние, и журнал.

---

## 15. FAQ и возможные проблемы

Сначала — вопросы про профиль пользователя (наследовано из дня 12).

| Проблема | Решение |
|---|---|
| Профиль сохранён, а ответ агента не изменился | Проверьте три вещи: (1) у агента именно тот `user_id` — карточка агента и блок «🔀 Профиль активного агента» (§6.5); (2) у профиля есть заполненные поля — предпросмотр «Что уходит в системный промпт» (§6.3) не должен быть пустым; (3) профиль не пустой: пустой профиль в промпт ничего не добавляет. Персонализация — это инструкция стилю, а не данные: фактических знаний она агенту не добавляет. |
| Профиль не появился в селекторе при создании агента | Список профилей читается при отрисовке сайдбара: нажмите «🔄 Обновить список» и откройте форму «➕ Новый агент» заново (§6.5). |
| `409` при создании профиля | Профиль с таким `user_id` уже есть. Откройте его в селекторе «Профиль» и сохраните форму (§6.3) либо возьмите другой `user_id`. |
| `404` на `/users/{user_id}/profile` | Профиля нет: создайте его формой «➕ Создать профиль» или кнопкой готового профиля (§6.2). `PUT` профиль не создаёт — он только заменяет настройки. |
| `422` при сохранении профиля | Проверьте значения: `tone` / `verbosity` / `language` / `format` — только из списка §6.3, длина ответа 20…8000, тем не больше 20 по 100 символов, дисклеймеров не больше 20 по 500, инструкций не больше 30 по 500 и не больше 4000 символов суммарно. Неизвестное поле тоже даёт `422` (схемы запрещают лишние поля). |
| Сохранил профиль, а часть настроек «сбросилась» | Так и задумано: сохранение формы — это `PUT`, полная замена. Поля, оставленные «— не задано —», становятся «не настроено» и в промпт не попадают. Заполните все нужные поля и сохраните снова. |
| Удалил профиль — агенты пропали? | Нет. Внешнего ключа между `user_profiles` и `agents` нет намеренно: агенты остаются работоспособными и просто теряют персонализацию (§4.1). Их диалог, задачи и долговременная память не затрагиваются. |
| Два профиля дали похожие ответы | Сравнение идёт по смыслу, а не по строке: посмотрите таблицы «Что повлияло на ответ» и системные промпты (§6.6) — если настройки различаются, а модель их проигнорировала, переформулируйте вопрос или сделайте настройки контрастнее (например, `plain text` + предел 600 символов против `markdown` + подробный ответ). Предел длины и формат модель соблюдает приблизительно. |
| Персонализация после «🆕 Новая сессия» исчезла | Не должна: новая сессия очищает только краткосрочный слой, профиль к нему не привязан (§4.4). Проверьте `user_id` агента и заполненность профиля, как в первом пункте; в отчёте §10 шаг (г) проверяет это же. |
| Плашка пишет «без персонализации» | Профиль не выбран, пуст или у выбранного `user_id` нет ни одной заполненной настройки — заполните форму (§6.3) и сохраните: следующий же запрос уйдёт с блоком профиля. |

Теперь — вопросы про слои памяти.

| Проблема | Решение |
|---|---|
| После «🆕 Новая сессия» диалога нет, но рабочая и долговременная память на месте | Так и задумано: сессия — это границы краткосрочной памяти. `POST /memory/session` удаляет реплики прошлой сессии, конспекты и факты, а рабочая (по `task_id`), долговременная (профиль/предпочтения/решения/знания) память и профиль пользователя к диалогу не привязаны. Индикатор после новой сессии показывает `0` записей в краткосрочном слое и прежние числа в двух других. |
| После новой сессии пропал конспект старого диалога — почему? | Конспект (`summaries`) — сжатие **краткосрочного** слоя, то есть производное от конкретного диалога. Если оставить его при смене сессии, устаревшее содержание продолжало бы подставляться в системное сообщение. Факты (`facts`) удаляются по той же причине. Если данные нужно сохранить между сессиями, перенесите их в долговременную память (категория `knowledge` или `preference`) — автоматического переноса нет. |
| Запись рабочей памяти «не сохранилась», а значение изменилось | Это upsert: пара «задача + ключ» уникальна, поэтому повторное сохранение того же ключа перезаписывает значение. Хотите две записи — используйте разные ключи (`цель` и `критерий_приёмки`). |
| Переключил задачу — рабочая память «опустела» | Записи привязаны к `task_id`, поэтому у новой задачи свой набор. Вернитесь к прежней задаче селектором «🔀 Переключить задачу» — записи на месте. Диалог при переключении не теряется. |
| Записей в долговременной памяти больше, чем видно в контексте | В запрос уходит до 5 записей (`LONG_TERM_LIMIT`): сначала совпавшие по ключевым словам запроса (и по упоминанию категории), затем самые уверенные. Поднять лимит — правка `LONG_TERM_LIMIT` в `backend/core/config.py`. |
| `422` при сохранении долговременной записи | Проверьте: категория — только `profile` / `preference` / `decision` / `knowledge`, `confidence` — от 0 до 1, ключ и значение непустые. Категория `profile` здесь — обычные данные агента, к профилю пользователя она отношения не имеет (§5.4, §6). |
| Записал в рабочую память, а индикатор показывает `0 записей` | Проверьте активную задачу (§5.5): запись ушла в другую задачу, либо это первая реплика сессии — в ней краткосрочный слой всегда `0` токенов, а рабочая метрика появится только после успешного хода. |

Теперь — вопросы про состояние задачи (день 13).

| Проблема | Решение |
|---|---|
| В системном промпте нет блока «Состояние задачи» | Блок подключается, только если состояние **есть** и задача **активна**. Проверьте: (1) состояние заведено — раздел «🧭 Состояние задачи» показывает этап и шаг, а `GET /tasks/{task_id}/state` отвечает `200`, а не `404` (§7.2); (2) активная задача агента — именно эта: строка «задача **task_id**» в сайдбаре и в панели совпадают (§5.5); (3) если состояния нет вовсе, исправлять нечего — заведите его кнопкой «➕ Создать задачу» или `POST /agents/{agent_id}/tasks`. Блок видно в ответе генерации: `record["system_prompt"]` и вкладка «🧩 Блок в системном промпте». |
| `409` при создании задачи: «Задача tz-portal уже существует» | `task_id` уникален **глобально**, а не в пределах агента: `task_states.task_id` — уникальный столбец. Работайте с существующей задачей (`GET /tasks/tz-portal/state`) либо возьмите другое имя. |
| `400` на переходе | Это отказ FSM с объяснением в теле, а не сбой. «задача завершена: этап done терминальный» — `advance` после `done`; «задача уже на паузе» / «задача не на паузе» — `pause`/`resume` не в том состоянии; «откат с этапа execution возможен только на этап planning» — `to_stage` не равен цели отката (`rollback_stage` в `GET /tasks/{task_id}/state`); «у этапа planning нет предыдущего этапа» — откат из `planning`, из `done` и с паузы не описан вовсе; «шаг … не принадлежит этапу …» — шаг из чужого этапа в теле `transition`. |
| Как продолжить работу после перезапуска бэкенда? | Ничего не делать специально: состояние читается из `day13/agents.db` при каждом запросе. Запустите uvicorn снова на той же базе — `GET /tasks/{task_id}/state` вернёт тот же этап и шаг, а реплика «продолжи» поставит задачу на них же (§7.9). Пауза перед остановкой не обязательна, но фиксирует место явно. |
| Реплика не меняет состояние задачи | Намерение ищется по фразам из §7.8 и на границе слова: «продолжительность сессии» — не намерение, а «продолжи» — намерение; «подтверждаю, продолжаем» распознаётся как продолжение (приоритет групп). Ещё три причины: состояния у задачи нет (§7.2), задача уже в `done` (репликами она не воскрешается) или переход недопустим для текущего этапа — тогда он пишется в лог, а запрос проходит как обычно. |
| «🧹 Очистить историю» и «🆕 Новая сессия» сбрасывают состояние задачи? | Нет. Обе операции работают с диалогом: чистят реплики, конспекты, факты, ветки и метрики, а таблицы `task_states`/`task_transitions` не трогают — как и рабочую и долговременную память. Состояние задачи удаляется только вместе с агентом («🗑 Удалить агента») или вручную в SQLite (§14). |

Общие вопросы:

| Проблема | Решение |
|---|---|
| «🔌 Бэкенд недоступен» в интерфейсе | Запустите бэкенд из папки `day13`: `uv run uvicorn backend.api.main:app --port 8000`. Проверьте адрес: он должен совпадать с `DAY13_BACKEND_URL` (по умолчанию `http://127.0.0.1:8000`). |
| Порт 8000 (или 8501) занят | Запустите на другом порту: `uvicorn backend.api.main:app --port 8019` и, для фронтенда, задайте `$env:DAY13_BACKEND_URL="http://127.0.0.1:8019"`; Streamlit — `--server.port 8502`. |
| Генерация вернула `502` «Ключ API не задан» | Ключа нет ни в `day13/.env`, ни в переменной `DEEPSEEK_API_KEY`. Сделайте `copy .env.example .env` и впишите `DEEPSEEK_API_KEY=sk-...`, затем перезапустите бэкенд. Карточки агентов, слои памяти, CRUD профилей, CRUD состояния задачи и скрипт с `--no-api` работают и без ключа. |
| Ошибка `429` (rate limit) от DeepSeek | Слишком много запросов подряд. Подождите и повторите; для демонстрации делайте паузы между ходами. Панель сравнения профилей тратит **два** запроса сразу, «⚖️ Сравнить режимы» с галочкой — тоже два. |
| `404` на агента | Агент был удалён или `agent_id` указан неверно. Обновите список кнопкой «🔄 Обновить список» и выберите агента заново. |
| `404` при удалении долговременной записи | Записи с таким `id` уже нет (например, её удалили в другой вкладке). Обновите страницу — селектор «Удалить запись по id» перечитает список. |
| `422` при создании/правке агента | Проверьте значения: `temperature` 0.0–2.0, `max_tokens` 1–8192, `keep_last_messages` 2–20, `summarize_every` 2–40, `user_id` — непустая строка до 64 символов. Такой ответ означает ошибку валидации тела запроса. |
| `agents.db` попал в git | Не должен: файлы `*.db` игнорируются. Если он уже отслеживается, уберите из индекса (`git rm --cached day13/agents.db`) — файл останется на диске. |
| Как сбросить эксперимент | «🧹 Очистить историю» удаляет реплики, конспекты, факты, ветки и метрики, сохраняя конфигурацию агента, рабочую и долговременную память и состояние задачи. Полный сброс — «🗑 Удалить агента» (вместе с ним уходят его задачи и журнал переходов) и создать заново; можно также остановить бэкенд и удалить `day13/agents.db` (таблицы создадутся заново). Профили пользователей живут в той же базе: чтобы начать «с чистого листа», удалите их в разделе «👤 Профиль пользователя». |

Вопросы по наследованным панелям дня 9–10:

| Проблема | Решение |
|---|---|
| Кнопка «Сжать сейчас» отвечает «Сжимать пока нечего» | Это нормально: непокрытых реплик меньше порога `summarize_every`, либо истории нет вовсе. Наберите диалог или уменьшите порог. Отчёт приходит с `created: false` и текстом причины. |
| «Чистая экономия» отрицательная | Конспект оказался дороже заменённых реплик — типично для коротких сообщений. Экономия контекста есть, но на сам конспект ушло больше токенов. Увеличьте `summarize_every` и/или `keep_last`, либо пишите более длинные реплики. |
| Где посмотреть состояние FSM сжатия | В панели «🗜 Сжатие контекста» — подпись «Состояние процесса: …» (`tracking`, `summary_pending`, `summarizing`, `error`). В БД оно не хранится: выводится из watermark, числа непокрытых реплик и порога. Подробнее — [`../../day10/docs/usage.md`](../../day10/docs/usage.md). |
| Оценки tiktoken не совпадают с `usage` API | Это ожидаемо: tiktoken (`cl100k_base`) — приблизительная оценка, у DeepSeek свой токенизатор. Для конкретного запроса/ответа верьте фактическим `usage` из API; на tiktoken построены расчёты контекста, экономии и разбивка по слоям. |

---

## 16. Ссылки

**Документы дня 13 (в `day13/docs/`):**

* [architecture.md](architecture.md) — компоненты, схема БД (11 таблиц, включая
  `task_states`/`task_transitions`, `user_profiles` и колонку `agents.user_id`),
  слои памяти, состояние задачи как FSM, персонализация, порядок блоков системного
  промпта, поток данных, тесты.
* [api.md](api.md) — эндпоинты с примерами запросов и ответов, формы данных, коды
  ошибок: `/memory/...` (слои), `/users`, `/users/{user_id}/profile`,
  `/agents/{agent_id}/profile`, `/agents/{agent_id}/tasks` и `/tasks/{task_id}/...`
  (состояние задачи).
* [`../STRUCTURE.md`](../STRUCTURE.md) — карта модулей дня и лимит размера файлов.

**Артефакты дня 13 (в `day13/`):**

* [`reports/task_state_demo.md`](reports/task_state_demo.md) — отчёт прогона состояния задачи:
  пять фаз в пяти отдельных процессах (пауза → перезапуск → продолжение),
  таблицы переходов, ответы агента, блок состояния из системного промпта и вывод
  о сохранении между запусками. Собирается скриптом
  `../task_state_demo.py` (§7.9), текст отчёта рисует
  `../task_demo_report.py`.
* `scripts/task_state_demo.py` — демонстрация дня 13: `--reset`, `--phase N`, `--all`,
  `--all --no-api`; работает на отдельной БД `task_state_demo.db` (агент
  `demo13`, задача `tz-portal`), вашу `agents.db` не затрагивает.
* [`reports/personalization_comparison.md`](reports/personalization_comparison.md) — отчёт
  прогона персонализации (наследовано из дня 12): таблица «Профиль | Настройки |
  Ответ агента | Какие элементы профиля повлияли», полные ответы с системными
  промптами, наблюдения и вывод. Собирается скриптом
  `../personalization_comparison.py` (§10), заглушку для офлайн-режима даёт
  `../comparison_stub.py`.
* `personalization_demo.db` — отдельная БД демонстрации персонализации, которую
  пересоздаёт скрипт отчёта.

**Артефакты дня 11 (в `day11/`, в `day13/` их нет):**

* [`../../day11/memory_layers_comparison.md`](../../day11/memory_layers_comparison.md)
  — отчёт дня 11 по трём слоям памяти: таблица слоёв, журнал прогона,
  доказательства после новой сессии. В дне 13 этот прогон заменён отчётами по
  состоянию задачи и персонализации, а слои проверяются индикатором §5.6 и
  тестами §10.

**Наследованное (дни 9–10):**

* [`../../day10/docs/usage.md`](../../day10/docs/usage.md) — наследованные
  возможности: стратегии контекста, сжатие, факты, ветки, токены (§13).
* [`../../day10/comparison.md`](../../day10/comparison.md) — прогон четырёх
  стратегий контекста дня 10.
