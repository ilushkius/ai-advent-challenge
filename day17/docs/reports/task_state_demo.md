# Отчёт дня 16 — состояние задачи как FSM

Прогон: 2026-09-18 14:27 UTC · режим: офлайн-заглушка (`--no-api`, без сети) · модель: **deepseek-chat**

Демо-база: `task_state_demo.db` · агент `demo15` · задача `tz-portal`.

Каждая фаза — ОТДЕЛЬНЫЙ процесс (`subprocess`), поэтому отчёт проверяет главное утверждение дня: состояние задачи читается из SQLite, а не из памяти процесса.

Запуск:

```bash
cd day16
uv run python scripts/task_state_demo.py --all           # с API-ключом
uv run python scripts/task_state_demo.py --all --no-api  # офлайн, без сети
uv run python scripts/task_state_demo.py --phase 2       # одна фаза
uv run python scripts/task_state_demo.py --reset         # очистить БД и отчёт
```

## Фаза 1 — постановка задачи и пауза (pid 28456)

Задача `tz-portal` заведена: **planning/gather_requirements**, ожидаемое действие — ожидается уточнение требований пользователем.

### Шаг 1 — обычная реплика (намерения нет)

Реплика: `Собери требования к задаче`

Ответ агента (обрезан):

```text
[офлайн-заглушка] запрос: Собери требования к задаче
[офлайн-заглушка] профиль в промпте: Текущий этап задачи: planning. Допустимые следующие этапы: paused. Ожидаемое действие: ожидается уточнение требований пользователем. Не пытайся перейти в недопустимый этап — сначала заверши текущий. Текущий шаг: gather_requirements. Предыдущие шаги: нет.
```

Состояние задачи после реплики: **planning/gather_requirements**

Блок состояния в системном промпте запроса:

```text
Состояние задачи (текущий этап и шаг; продолжай с этого места):
Текущий этап задачи: planning. Допустимые следующие этапы: paused. Ожидаемое действие: ожидается уточнение требований пользователем. Не пытайся перейти в недопустимый этап — сначала заверши текущий. Текущий шаг: gather_requirements. Предыдущие шаги: нет.
```

### Шаг 2 — реплика «подтверждаю» двигает шаг

Реплика: `подтверждаю`

Ответ агента (обрезан):

```text
[офлайн-заглушка] запрос: подтверждаю
[офлайн-заглушка] профиль в промпте: Текущий этап задачи: planning. Допустимые следующие этапы: paused. Ожидаемое действие: ожидается определение границ задачи агентом. Не пытайся перейти в недопустимый этап — сначала заверши текущий. Текущий шаг: define_scope. Предыдущие шаги: нет.
```

Состояние задачи после реплики: **planning/define_scope**

Блок состояния в системном промпте запроса:

```text
Состояние задачи (текущий этап и шаг; продолжай с этого места):
Текущий этап задачи: planning. Допустимые следующие этапы: paused. Ожидаемое действие: ожидается определение границ задачи агентом. Не пытайся перейти в недопустимый этап — сначала заверши текущий. Текущий шаг: define_scope. Предыдущие шаги: нет.
```

Прямой вызов `advance_task_step` (кнопка «Следующий шаг»): **planning/create_plan**.

### Шаг 3 — реплика «поставь задачу на паузу»

Реплика: `поставь задачу на паузу`

Ответ агента (обрезан):

```text
[офлайн-заглушка] запрос: поставь задачу на паузу
[офлайн-заглушка] профиль в промпте: Текущий этап задачи: paused. Допустимые следующие этапы: planning. Ожидаемое действие: задача на паузе; ожидается продолжение (resume). Не пытайся перейти в недопустимый этап — сначала заверши текущий. Текущий шаг: create_plan. Предыдущие шаги: нет.
```

Состояние задачи после реплики: **paused/create_plan**

Блок состояния в системном промпте запроса:

```text
Состояние задачи (текущий этап и шаг; продолжай с этого места):
Текущий этап задачи: paused. Допустимые следующие этапы: planning. Ожидаемое действие: задача на паузе; ожидается продолжение (resume). Не пытайся перейти в недопустимый этап — сначала заверши текущий. Текущий шаг: create_plan. Предыдущие шаги: нет.
```

Итог фазы (состояние ПЕРЕД перезапуском процесса): этап **paused**, шаг **create_plan** сохранён для продолжения.

После фазы 1: новый процесс (pid 28456), состояние прочитано из `task_state_demo.db` — **paused/create_plan**

## Фаза 2 — продолжение после перезапуска (pid 25204)

Состояние ДО любой реплики в этом процессе: **paused/create_plan**, `paused_from_stage` = `planning`.

Старт фазы 2: новый процесс (pid 25204), состояние прочитано из `task_state_demo.db` — **paused/create_plan**

### Шаг 1 — реплика «продолжи»

Реплика: `продолжи`

Ответ агента (обрезан):

```text
[офлайн-заглушка] запрос: продолжи
[офлайн-заглушка] профиль в промпте: Текущий этап задачи: planning. Допустимые следующие этапы: paused. Ожидаемое действие: ожидается утверждение плана пользователем. Не пытайся перейти в недопустимый этап — сначала заверши текущий. Текущий шаг: create_plan. Предыдущие шаги: нет.
```

Состояние задачи после реплики: **planning/create_plan**

Блок состояния в системном промпте запроса:

```text
Состояние задачи (текущий этап и шаг; продолжай с этого места):
Текущий этап задачи: planning. Допустимые следующие этапы: paused. Ожидаемое действие: ожидается утверждение плана пользователем. Не пытайся перейти в недопустимый этап — сначала заверши текущий. Текущий шаг: create_plan. Предыдущие шаги: нет.
```

Флаг «план утверждён», затем `advance_task_step`: **execution/implement** — задача вернулась на сохранённый шаг планирования, затем пошла дальше.

## Фаза 3 — выполнение и выход на валидацию (pid 24604)

Старт фазы 3: новый процесс (pid 24604), состояние прочитано из `task_state_demo.db` — **execution/implement**

`advance_task_step`: **execution/test_locally** — локальная проверка.

### Шаг 1 — реплика «готово» (после флага реализации)

Реплика: `готово`

Ответ агента (обрезан):

```text
[офлайн-заглушка] запрос: готово
[офлайн-заглушка] профиль в промпте: Текущий этап задачи: validation. Допустимые следующие этапы: execution, paused. Ожидаемое действие: ожидается ревью результата пользователем. Не пытайся перейти в недопустимый этап — сначала заверши текущий. Текущий шаг: review. Предыдущие шаги: planning (gather_requirements, define_scope, create_plan) — завершены; execution (implement, test_locally) — завершены.
```

Состояние задачи после реплики: **validation/review**

Блок состояния в системном промпте запроса:

```text
Состояние задачи (текущий этап и шаг; продолжай с этого места):
Текущий этап задачи: validation. Допустимые следующие этапы: execution, paused. Ожидаемое действие: ожидается ревью результата пользователем. Не пытайся перейти в недопустимый этап — сначала заверши текущий. Текущий шаг: review. Предыдущие шаги: planning (gather_requirements, define_scope, create_plan) — завершены; execution (implement, test_locally) — завершены.
```

## Фаза 4 — откат, возврат и завершение (pid 368)

Старт фазы 4: новый процесс (pid 368), состояние прочитано из `task_state_demo.db` — **validation/review**

`advance_task_step`: **validation/run_tests** — прогон тестов.

`rollback_task(execution)`: **execution/implement** — шаг сброшен на первый шаг этапа выполнения.

Четыре `advance_task_step` после отката: **validation/finalize** (implement → test_locally → review → run_tests → finalize).

`transition_task(done)`: **done/finalize**, задача активна: **False**.

Задача завершена — в журнале это отдельная причина, а не серия шагов.

## Фаза 5 — итог после пяти запусков (pid 18404)

Старт фазы 5: новый процесс (pid 18404), состояние прочитано из `task_state_demo.db` — **done/finalize**

Текущее состояние: **done/finalize**, ожидаемое действие — задача завершена; ожидается новая задача.

Задача в списке активных задач агента: **False** (список активных: пуст).

### Полный журнал переходов

| # | из | в | причина | время |
|---|---|---|---|---|
| 1 | — (создание) | `planning/gather_requirements` | задача создана | 2026-09-18 14:27:19 |
| 2 | `planning/gather_requirements` | `planning/define_scope` | следующий шаг | 2026-09-18 14:27:19 |
| 3 | `planning/define_scope` | `planning/create_plan` | следующий шаг | 2026-09-18 14:27:19 |
| 4 | `planning/create_plan` | `paused/create_plan` | пауза | 2026-09-18 14:27:19 |
| 5 | `paused/create_plan` | `planning/create_plan` | продолжение после паузы | 2026-09-18 14:27:21 |
| 6 | `planning/create_plan` | `execution/implement` | следующий шаг | 2026-09-18 14:27:21 |
| 7 | `execution/implement` | `execution/test_locally` | следующий шаг | 2026-09-18 14:27:22 |
| 8 | `execution/test_locally` | `validation/review` | следующий шаг | 2026-09-18 14:27:22 |
| 9 | `validation/review` | `validation/run_tests` | следующий шаг | 2026-09-18 14:27:24 |
| 10 | `validation/run_tests` | `execution/implement` | откат на предыдущий этап | 2026-09-18 14:27:24 |
| 11 | `execution/implement` | `execution/test_locally` | следующий шаг | 2026-09-18 14:27:24 |
| 12 | `execution/test_locally` | `validation/review` | следующий шаг | 2026-09-18 14:27:24 |
| 13 | `validation/review` | `validation/run_tests` | следующий шаг | 2026-09-18 14:27:24 |
| 14 | `validation/run_tests` | `validation/finalize` | следующий шаг | 2026-09-18 14:27:24 |
| 15 | `validation/finalize` | `done/finalize` | задача завершена | 2026-09-18 14:27:24 |

Всего переходов в журнале: **15**.

### Вывод

Состояние задачи сохранено между пятью запусками процесса: фаза 1 оставила задачу на паузе в `paused/create_plan`, фаза 2 в новом процессе продолжила её с этого же шага, фазы 3–4 довели до `done`, а фаза 5 прочитала итог из `task_state_demo.db`. Ни один процесс не держал состояние в памяти: всё, что видно в журнале, — строки таблиц `task_states` и `task_transitions`.

Воспроизведение:

```bash
cd day16
uv run python scripts/task_state_demo.py --all           # нужен DEEPSEEK_API_KEY в day16/.env
uv run python scripts/task_state_demo.py --all --no-api  # офлайн-заглушка, без сети
```

