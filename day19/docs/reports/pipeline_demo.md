# Композиция MCP-инструментов: прогон четырёх сценариев (день 19)

Прогон: 2026-09-24T16:29:03+00:00 · сводка: агрегация (LLM выключена) · источник: заметки дня `mcp_server/data/notes.md`

## 1. Проверки прогона

| сценарий | проверка | результат |
|---|---|---|
| 1. Успешный пайплайн (search → summarize → save_to_file) | прогон завершён со статусом completed | ✅ |
| 1. Успешный пайплайн (search → summarize → save_to_file) | шагов ровно три и все со статусом ok | ✅ |
| 1. Успешный пайплайн (search → summarize → save_to_file) | данные доехали до второго шага (items непусты) | ✅ |
| 1. Успешный пайплайн (search → summarize → save_to_file) | сводку собрал движок агрегации | ✅ |
| 1. Успешный пайплайн (search → summarize → save_to_file) | файл сохранён в каталоге вывода | ✅ |
| 1. Успешный пайплайн (search → summarize → save_to_file) | размер файла совпадает с ответом инструмента | ✅ |
| 2. Пустой результат поиска — досрочное завершение | прогон завершён со статусом stopped | ✅ |
| 2. Пустой результат поиска — досрочное завершение | ошибки шага нет (это не сбой, а решение не звать инструмент) | ✅ |
| 2. Пустой результат поиска — досрочное завершение | третий шаг не запускался (в журнале два шага) | ✅ |
| 2. Пустой результат поиска — досрочное завершение | шаг summarize остановлен условием «нет данных для обработки» | ✅ |
| 2. Пустой результат поиска — досрочное завершение | итоговое сообщение — сообщение условия | ✅ |
| 2. Пустой результат поиска — досрочное завершение | файл не создан | ✅ |
| 3. Ошибка на втором шаге (summarize) | прогон остановлен со статусом failed | ✅ |
| 3. Ошибка на втором шаге (summarize) | прогон остановился на шаге 1 | ✅ |
| 3. Ошибка на втором шаге (summarize) | первый шаг остался успешным | ✅ |
| 3. Ошибка на втором шаге (summarize) | второй шаг не выполнен (отказ по аргументам) | ✅ |
| 3. Ошибка на втором шаге (summarize) | третий шаг не запускался (шагов два) | ✅ |
| 3. Ошибка на втором шаге (summarize) | файл не создан | ✅ |
| 3. Ошибка на втором шаге (summarize) | ошибка инструмента — тоже данные: прогон failed, есть текст причины | ✅ |
| 4. Запуск пайплайна через агента | пайплайн распознан в реплике | ✅ |
| 4. Запуск пайплайна через агента | прогон завершён успешно | ✅ |
| 4. Запуск пайплайна через агента | результат прогона ушёл в системный промпт | ✅ |
| 4. Запуск пайплайна через агента | блок пайплайна виден в системном промпте | ✅ |
| 4. Запуск пайплайна через агента | одиночный вызов инструмента не понадобился | ✅ |
| 4. Запуск пайплайна через агента | ответ агента получен (офлайн-заглушка модели) | ✅ |
| 4. Запуск пайплайна через агента | обычная реплика пайплайн не запускает | ✅ |

**Итого: 26 из 26 проверок пройдено.**

## 2. Инструменты композиции

Сервер дня публикует девять инструментов; пайплайн использует три из них (остальные шесть — данные и планировщик дней 17–18).

| инструмент | параметры | что возвращает | роль в пайплайне |
|---|---|---|---|
| `search` | `query`*: string, `source`: string, `limit`: integer | query, source, source_kind, count, items | шаг 1: находит элементы в источнике (лента API, файл дня, SQLite дня) |
| `summarize` | `items`*: array<object>, `style`: string, `max_length`: integer | summary_text, key_points, total_items, style_used, engine | шаг 2: сводит найденные элементы и выделяет ключевые пункты |
| `save_to_file` | `content`*: string, `filename`: string, `format`: string | filename, filepath, size_bytes, format, saved_at | шаг 3: пишет текст сводки в файл каталога output/ |

`*` — обязательный параметр (`inputSchema.required`).

## 3. Сценарий 1: успешный пайплайн (search → summarize → save_to_file)

Прогон №1 · статус `completed` · длительность 271 мс · итог: пайплайн выполнен

### Трассировка шагов

| шаг | инструмент | входные данные | выходные данные | время выполнения | статус |
|---|---|---|---|---|---|
| 0 | `search` | `{"query": "RAG", "source": "file:mcp_server/data/notes.md", "limit": 5}` | `{"reason_code": null, "structured": {"query": "RAG", "source": "file:mcp_server/data/notes.md", "source_kind": "file", "count": 5, "items": [{"id": "file:mcp_server/data/notes.md#3", "title": "## RAG: что это", "content"…` | 105 мс | ✅ ok |
| 1 | `summarize` | `{"items": [{"id": "file:mcp_server/data/notes.md#3", "title": "## RAG: что это", "content": "## RAG: что это", "url": "", "metadata": {"path": "mcp_server/data/notes.md", "block": 3, "total_blocks": 16}}, {"id": "file:mc…` | `{"reason_code": null, "structured": {"summary_text": "Найдено 5 элементов. Первые: ## RAG: что это; RAG (retrieval-augmented generation) — схема, в которой модель отвечает не по памяти, а по; ## RAG: чанкинг и размер фра…` | 7 мс | ✅ ok |
| 2 | `save_to_file` | `{"content": "Найдено 5 элементов. Первые: ## RAG: что это; RAG (retrieval-augmented generation) — схема, в которой модель отвечает не по памяти, а по; ## RAG: чанкинг и размер фрагмента", "filename": "run-success.md", "f…` | `{"reason_code": null, "structured": {"filename": "run-success.md", "filepath": "C:/Users/ilushkius/ai-advent-challenge/day19/output/run-success.md", "size_bytes": 266, "format": "md", "saved_at": "2026-09-24T16:24:25+00:…` | 8 мс | ✅ ok |

Данные между инструментами передаются маппингом: поле `$steps.0.structured.items` — это найденные элементы (первый шаг), `$steps.1.structured.summary_text` — текст сводки (второй шаг). Каждый шаг видит только выходы предыдущих, поэтому порядок в конфигурации и есть поток данных.

### Сохранённый файл (пример из day19/output/)

Файл `run-success.md` · 266 байт · формат `md` · записан 2026-09-24T16:24:25+00:00

```markdown
Найдено 5 элементов. Первые: ## RAG: что это; RAG (retrieval-augmented generation) — схема, в которой модель отвечает не по памяти, а по; ## RAG: чанкинг и размер фрагмента
```

Каталог вывода прогона: `C:/Users/ilushkius/ai-advent-challenge/day19/output`.

## 4. Сценарий 2: пустой результат поиска — досрочное завершение

Запрос `квантовые вычисления` в заметках дня не встречается, поэтому первый шаг вернул пустой список, а условие шага `summarize` (`$steps.0.structured.items` + `non_empty`) не выполнилось.

| что | значение |
|---|---|
| статус прогона | `stopped` |
| итоговое сообщение | нет данных для обработки |
| номер упавшего шага | None (None — это не ошибка) |
| последний шаг | `summarize` — ⏹ stopped |
| сообщение шага | нет данных для обработки |
| файл создан | нет |

Разница между `stopped` и `failed` — содержательная: нечего сводить это не сбой инструмента, а решение не звать его вовсе, и в промпт агента такое досрочное завершение попадает как итог прогона (модель не должна отвечать так, будто сводка есть).

## 5. Сценарий 3: ошибка на втором шаге (summarize)

Две причины остановки на одном и том же шаге — до вызова и внутри вызова:

| причина | что произошло | как видно в журнале |
|---|---|---|
| негодные аргументы | шаг просит `items`, а маппинг отдал строку запроса (`$steps.0.structured.query`) | `reason_code = bad_arguments`, вызов до сервера не дошёл |
| ошибка инструмента | аргументы верные, стиль `exotic` не поддержан | `is_error = true`, текст причины в `output_result.text` и `error` шага |

| шаг | инструмент | входные данные | выходные данные | время выполнения | статус |
|---|---|---|---|---|---|
| 0 | `search` | `{"query": "RAG", "source": "file:mcp_server/data/notes.md", "limit": 5}` | `{"reason_code": null, "structured": {"query": "RAG", "source": "file:mcp_server/data/notes.md", "source_kind": "file", "count": 5, "items": [{"id": "file:mcp_server/data/notes.md#3", "title": "## RAG: что это", "content"…` | 5 мс | ✅ ok |
| 1 | `summarize` | `{"items": "RAG", "style": "short", "max_length": 600}` | `{"reason_code": "bad_arguments", "structured": null, "text": "", "is_error": true}` | 0 мс | ❌ failed |

Статус прогона: `failed`, остановился на шаге 1; третий шаг не запускался — ошибка любого шага останавливает пайплайн и логируется с контекстом (входные аргументы, результат, время, текст ошибки).

Вариант с ошибкой инструмента: `failed`, текст причины — `Error executing tool summarize: Стиль «exotic» не поддержан. Допустимы: short, detailed, bullets`.

## 6. Сценарий 4: запуск пайплайна через агента

Реплика: `найди статьи про RAG, сделай сводку и сохрани в файл`. Эвристика домена находит фразы всех трёх действий (поиск, сводка, сохранение) и запускает встроенный пайплайн синхронно — ответ агента должен опираться на данные этого же хода.

| что | значение |
|---|---|
| пайплайн распознан | `True` |
| статус прогона | `completed` |
| результат ушёл в промпт | `True` |
| добавлено токенов | 138 |
| одиночный вызов инструмента | не понадобился (`record['mcp']` = None) |
| обычная реплика | пайплайн не запущен (`detected = False`) |

Блок, который увидела модель (фрагмент системного промпта):

```
[офлайн-заглушка] результат пайплайна в запросе:
## Результат пайплайна
Пайплайн: search-summarize-save
Шаги:
- search: найдено 5 элементов (источник file: file:mcp_server/data/notes.md)
- summarize: 5 ключевых пунктов, стиль short, движок aggregation
- save_to_file: файл rag.md (266 байт, md)
Итог: пайплайн выполнен
Используй эти данные как источник правды в ответе и не выдумывай поля, которых здесь нет.
```

## 7. Схема таблиц пайплайна

### `pipeline_runs` — запуски пайплайна

| колонка | тип | смысл |
|---|---|---|
| `id` | INTEGER PK | номер запуска (им опрашивают статус) |
| `pipeline_name` | VARCHAR(100) | имя конфигурации пайплайна |
| `status` | VARCHAR(16) | статус: running | completed | stopped | failed |
| `started_at` | DATETIME | когда прогон начался (UTC) |
| `finished_at` | DATETIME | когда завершился (NULL — идёт) |
| `total_duration_ms` | INTEGER | длительность прогона в миллисекундах |

### `pipeline_steps` — шаги прогона

| колонка | тип | смысл |
|---|---|---|
| `id` | INTEGER PK | номер строки журнала |
| `run_id` | INTEGER FK → pipeline_runs | запуск (ON DELETE CASCADE) |
| `step_index` | INTEGER | номер шага в конфигурации (с нуля) |
| `tool_name` | VARCHAR(64) | MCP-инструмент шага |
| `input_args` | JSON | аргументы вызова (ссылки пайплайна уже разрешены) |
| `output_result` | JSON | результат: structured, text, reason_code, is_error, duration_ms |
| `duration_ms` | INTEGER | сколько миллисекунд занял шаг |
| `status` | VARCHAR(16) | статус шага: ok | failed | stopped |
| `error_message` | TEXT | причина остановки или сообщение условия перехода |

Связь: `pipeline_steps.run_id` → `pipeline_runs.id` с `ON DELETE CASCADE` (удаление запуска уносит его шаги). Прогон идёт в фоновом потоке бэкенда, поэтому журнал лежит в SQLite: интерфейс опрашивает статус и видит прогресс, пока шаги выполняются.

## 8. Автотесты дня

```
1789 passed, 269 warnings in 272.63s (0:04:32)
```

## 9. Что осталось за рамками дня

**Ветвления и параллельные шаги.** Пайплайн — линейная цепочка: шаг идёт за шагом, а условие может только остановить прогон целиком. Ветки (выбор продолжения по значению) и параллельный запуск независимых шагов не сделаны.

**Повторы упавших шагов.** Ошибка шага останавливает прогон. Политик retry, таймаутов на шаг и компенсации побочных эффектов (удалить созданный файл при отказе) в дне нет.

**Возобновление с места остановки.** Повторный запуск — это новый прогон: «продолжить с шага 2» по уже записанным результатам не реализовано.

**Векторный источник поиска.** Источник `sqlite:` ищет подстрокой (LIKE), эмбеддингов и векторного индекса в дне нет: заметки про RAG описывают приём, а не реализуют его.

**Расписание пайплайнов.** Пайплайн запускается по реплике или через API. Ставить его в планировщик (день 18) как периодическую задачу день не умеет: инструменты планировщика вызывают свои действия, а не пайплайн.

**Аутентификация API.** Эндпоинты `/pipelines` открыты, как и остальные: день рассчитан на локальный запуск.
