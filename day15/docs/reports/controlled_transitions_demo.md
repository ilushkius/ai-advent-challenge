# Отчёт дня 15 — контролируемые переходы состояний

Прогон: 2026-09-18 14:29 UTC · режим: **офлайн-заглушка, без сети** · модель: **deepseek-chat**

Демо-база: `controlled_transitions_demo.db` · агент `demo15` · задача `tz-transitions`.

Фазы 1 и 2 — РАЗНЫЕ процессы (`subprocess`), поэтому отчёт проверяет два утверждения дня сразу: правила допуска отклоняют недопустимый переход с объяснением, а пауза живёт в БД (колонка `task_states.paused_from_stage`), а не в памяти процесса.

Запуск:

```bash
cd day15
uv run python scripts/controlled_transitions_demo.py --all     # прогон и отчёт
uv run python scripts/controlled_transitions_demo.py --phase 1 # только фаза 1
uv run python scripts/controlled_transitions_demo.py --phase 2 # только фаза 2
uv run python scripts/controlled_transitions_demo.py --reset   # очистить прогон
```

## Сценарии перехода

| Попытка перехода | Допустимость | Причина отказа | Что предложил агент | Как продолжил после паузы |
|---|---|---|---|---|
| `planning → done` | недопустим | «Нельзя перейти из planning в done: пропущены этапы execution и validation» | — | — |
| `planning → execution` без согласования плана | недопустим | «Нельзя перейти в execution: план не утверждён» | — | — |
| реплика «подтверждаю» на последнем шаге планирования | недопустим | «Нельзя перейти в execution: план не утверждён» | — | — |
| `planning → execution` после флага `plan_approved` | допустим | — | — | — |
| `execution → validation` без согласования реализации | недопустим | «Нельзя перейти в validation: реализация не завершена» | — | — |
| `execution → нет-такого` | недопустим | «Неизвестный этап задачи: 'нет-такого'. Допустимые: planning, execution, validation, done, paused» | — | HTTP-контракт отклоняет такой этап кодом 422 (проверка схемы) |
| `execution → validation` после флага `implementation_complete` | допустим | — | — | — |
| `validation → paused` | допустим | — | — | пауза с этапа `validation` на шаге `review` (сохранён в `current_step`) |
| `paused → validation` (продолжение) | допустим | — | — | новый процесс вернул тот же этап и шаг: `validation/review` |
| предложение модели в ответе на этапе валидации | недопустим | «Нельзя перейти в done: валидация не пройдена» | «Задача завершена, можно сдавать.» → `done` | — |
| `validation → done` после флага `validation_passed` | допустим | — | — | — |
| `done → paused` | недопустим | «Нельзя перейти из done: этап done терминальный» | — | этап `done` терминальный: переходов из него нет вовсе |

## Фаза 1 — правила допуска в работе (pid 24448)

Задача `tz-transitions` заведена: **planning/gather_requirements**. Допустимые следующие этапы: `paused` — всё остальное отклоняется с причиной.

Ответ агента на реплику, которую правила не пустили (состояние задачи не изменилось):

```text
⚠️ Переход по реплике «advance» не выполнен: Нельзя перейти в execution: план не утверждён Доступные следующие этапы: paused.

Принято, продолжаю по плану.
```

Состояние после отклонённой реплики: **planning/create_plan**; `task_intent` = `{"intent": "advance", "applied": false, "reason": "Нельзя перейти в execution: план не утверждён", "allowed_next": ["paused"]}`.

Флаг «план утверждён» выставлен — переход выполнен: **execution/implement**.

Задача поставлена на паузу в процессе (pid 24448): **paused/review**, `paused_from_stage` = `validation`.

## Фаза 2 — продолжение в новом процессе (pid 12464)

Состояние до любой реплики в этом процессе: **paused/review**, `paused_from_stage` = `validation` — прочитано из `controlled_transitions_demo.db`, а не из памяти предыдущего процесса.

Отказ на предложение модели (ответ заглушки «Задача завершена, можно сдавать.») — переход не выполнен, вместо предложения уходит объяснение:

```text
🚧 Ответ предлагает переход в done, но это недопустимо. Нельзя перейти в done: валидация не пройдена Отметьте флаг «✅ Валидация пройдена» в панели задачи.
```

`task_proposal` = `{"proposed": "done", "allowed_next": ["execution", "paused"], "reason": "Нельзя перейти в done: валидация не пройдена", "hint": "Отметьте флаг «✅ Валидация пройдена» в панели задачи."}`, состояние по-прежнему **validation/review**.

Валидация согласована, задача завершена: **done/finalize**, активна — **False**. Попытка приостановить завершённую задачу отклонена: «Нельзя перейти из done: этап done терминальный».

## Продолжение после паузы

* фаза 1 (pid 24448) оставила задачу на **paused/review**, `paused_from_stage` = `validation`;
* фаза 2 (pid 12464) прочитала состояние из `controlled_transitions_demo.db` — этап и шаг **validation/review** — и продолжение вернуло ровно это место.

Утверждение: точка возврата хранится в колонке строки задачи, поэтому перезапуск процесса её не теряет, а шаг паузы (`current_step`) пауза не меняет.

## Журнал попыток

Записей в журнале: **15** (состоявшихся переходов — 9, отклонённых попыток — 6). Строки с `accepted = False`:

| # | этап | шаг | цель | причина отказа | время |
|---|---|---|---|---|---|
| 1 | `planning` | `gather_requirements` | `done` | Нельзя перейти из planning в done: пропущены этапы execution и validation | 2026-09-18 14:29:37 |
| 2 | `planning` | `gather_requirements` | `execution` | Нельзя перейти в execution: план не утверждён | 2026-09-18 14:29:37 |
| 3 | `planning` | `create_plan` | `execution` | Нельзя перейти в execution: план не утверждён | 2026-09-18 14:29:37 |
| 4 | `execution` | `test_locally` | `validation` | Нельзя перейти в validation: реализация не завершена | 2026-09-18 14:29:38 |
| 5 | `execution` | `test_locally` | `— не названа` | Неизвестный этап задачи: 'нет-такого'. Допустимые: planning, execution, validation, done, paused | 2026-09-18 14:29:38 |
| 6 | `done` | `finalize` | `paused` | Нельзя перейти из done: этап done терминальный | 2026-09-18 14:29:41 |

Каждая отклонённая попытка объясняет себя причиной отказа: состояние задачи такая строка не меняет (см. `task_states` — этап и шаг прежние), поэтому журнал — это ещё и материал для разбора «почему не пустило».

## Вывод

1. Переход этапа проверяется графом допуска: `planning → done` отклонён с перечислением пропущенных этапов, откат через два этапа — с указанием идти по одному, неизвестный этап — с перечислением допустимых.
2. Переход вперёд требует согласования этапа: без `plan_approved`, `implementation_complete` и `validation_passed` он отклоняется, называя флаг; выставление флага открывает ровно тот же переход.
3. Недопустимое предложение модели не выполняется и не остаётся в ответе: вместо него уходит отказ с причиной и подсказкой, а `task_proposal` попадает в отчёт генерации.
4. Пауза переживает перезапуск процесса: фаза 2 прочитала из БД тот же этап и шаг, на которых задача была приостановлена.
5. Этап `done` терминальный: из него нет переходов — даже на паузу.

Итоговое состояние задачи: **done/finalize**, активна — **False**.
