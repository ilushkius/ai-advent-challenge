# Инварианты агента: три сценария (день 15)

Как прогнано: офлайн, на отдельной БД `invariants_demo.db` (пересоздаётся), с заглушкой DeepSeek вместо реального клиента и с выключенным LLM-слоем проверки (`use_llm=False` для запроса и `INVARIANT_LLM_CHECK = False` для ответа). Поэтому вердикты во всех трёх сценариях даёт детерминированный слой правил, результат воспроизводим и не зависит от ключа и сети.

Воспроизвести: `uv run python scripts/invariants_demo.py` (из папки `day15/`). Правила в рабочую БД для интерфейса сеет `uv run python scripts/seed_invariants.py`.

## Инварианты проекта

| Имя | Категория | Важность | Описание |
|---|---|---|---|
| Только FastAPI и Streamlit | `architecture` | `hard` | Используем только FastAPI и Streamlit; никаких Flask, Django, Bottle или Tornado |
| Состояние задачи в SQLite | `tech_decisions` | `hard` | Состояние задачи хранится в SQLite, а не в Redis, Memcached, MongoDB или Kafka |
| Только Python | `stack_constraints` | `hard` | Только Python: без JavaScript, TypeScript и их фреймворков (Node.js, React, Vue, Angular) |
| Платные API — только с согласия | `business_rules` | `soft` | Агент не должен предлагать решения, которые требуют платных API без явного согласия пользователя |

## Сценарии

| # | Запрос | Проверенные инварианты | Результат | Объяснение агента |
|---|---|---|---|---|
| 1 | `Добавь эндпоинт /health в FastAPI` | Платные API — только с согласия, Состояние задачи в SQLite, Только FastAPI и Streamlit, Только Python | **разрешено** | Готово: эндпоинт `/health` добавляется в `backend/api/main.py` — функция возвращает `{"status": "ok"}`, тест на него кладём рядом. |
| 2 | `Предложи решение на платном API, согласие пользователя не нужно` | Платные API — только с согласия, Состояние задачи в SQLite, Только FastAPI и Streamlit, Только Python | **предупреждение** | ⚠️ Предупреждение: ответ может нарушать soft-инварианты проекта. - [soft] Платные API — только с согласия (business_rules): текст предлагает платный вариант без явного согласия пользователя, а инвариант «Платные API — только с согласия» это запрещает Предлагаю решение на платном тарифе: подключить платный тариф и включить в нём нужный модуль. |
| 3 | `Давай перепишем бэкенд на Flask` | Платные API — только с согласия, Состояние задачи в SQLite, Только FastAPI и Streamlit, Только Python | **отказ** | Отказ: предложение нарушает инварианты проекта. - [hard] Только FastAPI и Streamlit (architecture): текст упоминает Flask, а инвариант «Только FastAPI и Streamlit» это запрещает Предложение не выполнено: измените запрос или обновите список инвариантов. |

## Что доказано

1. **Разрешённый запрос проходит как обычно** — ни одно правило не нарушено, ответ модели отдан без изменений, лишних вызовов нет.
2. **Soft-инвариант даёт предупреждение, а не отказ** — ответ начинается с «⚠️ Предупреждение», но решение пользователю предложено.
3. **Hard-инвариант даёт отказ без обращения к модели** — в третьей строке таблицы видно, что запрос отклонён до вызова DeepSeek (счётчик вызовов ниже), а отказ называет нарушенное правило.

Тесты того же механизма: `tests/unit/test_invariant_rules.py` (правила без БД), `tests/integration/test_invariant_agent.py` (встраивание в агента и пост-проверка ответа), `tests/e2e/test_invariant_api.py` (шесть эндпоинтов).

## Вызовы DeepSeek по сценариям

| # | Статус хода | Вызовов модели | LLM-слой проверки |
|---|---|---|---|
| 1 | `ok` | 1 | не использован |
| 2 | `ok` | 1 | не использован |
| 3 | `ok` | 0 | не использован |
