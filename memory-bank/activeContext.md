# Active Context — текущее состояние и фокус

## Статус (на 2026-09-02)
- Последний коммит: `5aa6898` «Day 3 Refactoring» (2026-09-02 22:08), отправлен
  в origin. Рабочее дерево **чистое**.
- Дни 1–3 завершены; «наведение порядка» (README.md, shared/, memory-bank,
  рефакторинг day2/day3, day3/.env.example) закоммичено тем же коммитом.
  Ведётся подготовка к **Дню 4**.
- Memory Bank создан и ведётся (`.clinerules/memory-bank.md` + `memory-bank/*.md`).

## Недавние изменения (эта сессия)
- Добавлен `.clinerules/memory-bank.md` (правила работы с памятью).
- Созданы и заполнены: `projectbrief.md`, `productContext.md`, `systemPatterns.md`,
  `techContext.md`, `activeContext.md`, `progress.md`.
- «Наведение порядка» (коммит `5aa6898` «Day 3 Refactoring»): созданы
  `README.md`, `shared/deepseek_utils.py`, `day3/.env.example`; `day2/app.py` и
  `day3/app.py` переведены на импорт общих утилит (проверено py_compile +
  streamlit.testing.AppTest).

## Что реализовано (кратко)
- **День 1** — консольный потоковый чат с DeepSeek (`day1/deepseek_chat.py`).
- **День 2** — Streamlit-демо параметров генерации (`day2/app.py`, 399 строк).
- **День 3** — Streamlit-демо четырёх способов рассуждения (`day3/app.py`, 613
  строк), включая двухэтапный мета-промпт.

## Текущий фокус
1. Выбрать тему **Дня 4** и реализовать следующее самостоятельное демо.
2. ✅ «Наведение порядка» закоммичено (`5aa6898`) — можно начинать День 4.

## Активные решения / развилки
1. **Тема Дня 4 — не определена.** Кандидаты (см. итог сессии для автора):
   - RAG/работа с документами (embeddings, поиск по своим текстам);
   - Function Calling / Tools (API-вызовы из ответа модели);
   - многоходовый диалог с историей (сейчас истории нет даже в Дне 1);
   - сравнение `deepseek-chat` vs `deepseek-reasoner` на задачах рассуждения;
   - единое приложение-«хаб» по всем дням;
   - self-consistency / ансамбли ответов.
2. **Дублирование кода** приложений: хелперы вынесены в общий `shared/`
   (`read_key_from_env_file`, `parse_stop_sequences`, `usage_to_dict`, endpoint).
   По-прежнему дублируются CSS-блок и часть UI-разметки day2 ↔ day3 — решение
   «вынести в shared или оставить в днях» пока не принято.
3. **Формат демо** — консоль (Д1) → Streamlit (Д2+): решение принято ради
   наглядности (вкладки, метрики, JSON-лог «для видео и документации»).
4. **Порядок поиска ключа** — `.env` → env-переменная → ручной ввод (единый для всех дней).

## Важные паттерны и предпочтения (стиль автора)
- Один файл = одно приложение дня; «чистая» логика — в функциях, UI — в теле модуля.
- Параметры запроса собираются в единый dict для вызова и лога (не расходятся).
- Текст модели экранируется `html.escape` перед выводом через markdown.
- Промпты/хинты/подписи — константы-словари вверху файла; UI-тексты на русском.
- Комментарии — обучающие, объясняют «почему», а не только «что».

## Инсайты/грабли, подтверждённые кодом
- База DeepSeek — строго `https://api.deepseek.com` (без `api.` не работает).
- JSON-режим требует system-промпт + `response_format={"type":"json_object"}`.
- `deepseek-reasoner` может игнорировать `temperature`/`response_format`.
- `seed` при фиксированных прочих параметрах даёт детерминизм «символ в символ».
- Для двухэтапного мета-промпта нужен запас токенов на Шаге А (600).
- День 1 не хранит историю диалога — модель «не помнит» предыдущие реплики.

## Следующие шаги
1. Определить тему Дня 4 (уточнить у автора).
2. Реализовать День 4 в новой папке `day4/` (по образцу day2/day3).
3. После работы над Днём 4 — обновить Memory Bank (команда «update memory bank»).
