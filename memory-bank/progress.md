# Progress — что сделано, статус, проблемы

## Общий статус (на 2026-09-02)
| День | Дата | Коммит | Артефакт | Статус |
|---|---|---|---|---|
| 1 | 2026-08-31 | `2b1d7fb` | `day1/deepseek_chat.py` (184 стр.) | ✅ готово |
| 2 | 2026-09-01 | `9f3d20d` | `day2/app.py` (399 стр.) | ✅ готово |
| 3 | 2026-09-02 | `010c927` | `day3/app.py` (613 стр.) | ✅ готово |
| 4 | — | — | — | ⏳ следующий шаг |

## День 1 — «Первый запрос к DeepSeek» (консольный чат)
Реализовано:
- потоковый чат с `deepseek-chat` (`stream=True`, вывод по кусочкам);
- поиск ключа: `.env` → env-переменная → скрытый ввод `getpass`;
- защита от заглушек `is_real_key` (префикс `sk-`, отсев слов-плейсхолдеров);
- UTF-8-вывод для консоли Windows; цикл с выходом `exit`/`выход`;
  обработка ошибок API.
Работает: ✅ (локально есть `day1/.env`, скрипт запускался).
Ограничение: **история диалога не хранится** — каждый ход уходит отдельным
user-сообщением, модель не помнит контекст беседы.

## День 2 — «Формат ответа» (Streamlit, 399 строк)
Реализовано:
- управление `temperature`, `max_tokens`, `seed`, `stop`-строками,
  режим «Обычный текст / Строгий JSON»;
- JSON-режим: автодобавление `SYSTEM_PROMPT_JSON` + `response_format`;
- вкладки: «Результат» (теги `finish_reason`, токены, время, `st.json` либо карточка
  с `html.escape`) и «Технический лог» (что ушло в API, таблица влияния параметров,
  готовый JSON для копирования);
- валидации и понятные `st.error` вместо traceback.
Работает: ✅ (venv в `day2/.venv`, пакеты установлены; локального `.env` нет — ключ
вводится в UI либо берётся из env-переменной).
Нюансы: `deepseek-reasoner` игнорирует часть параметров (отмечено в help);
`.env` ищется в CWD → запускать `streamlit run` из папки `day2`.

## День 3 — «Способы рассуждения ИИ» (Streamlit, 613 строк)
Реализовано:
- четыре стратегии на задаче «куры и кролики»: Zero-Shot, Chain-of-Thought,
  Мета-промпт (Prompt-v-Prompt), Консилиум экспертов (Persona);
- конфиги-словари `REASONING_*`; двухэтапный мета-промпт `run_meta_stage`
  (Шаг А — модель пишет промпт, `META_MAX_TOKENS=600`; Шаг Б — решает по нему);
- наследование настроек Дня 2 (temp/seed/max_tokens/stop/JSON поверх способа);
- лог обоих этапов; предупреждение, если промпт Шага А обрезан лимитом.
Работает: ✅ по коду. Запуск в этой сессии не проверялся: в `day3` нет своего
`.venv`/`.env` (можно запустить через venv из `day2`, ключ — ввести в UI).

## Наведение порядка (2026-09-02; коммит `5aa6898` «Day 3 Refactoring»)
- корневой `README.md` — структура проекта, запуск, секреты, Memory Bank;
- общий пакет `shared/deepseek_utils.py` (+ `shared/__init__.py`): константа
  `DEEPSEEK_BASE_URL`, `read_key_from_env_file()`, `parse_stop_sequences()`,
  `usage_to_dict()`;
- `day2/app.py` и `day3/app.py` отрефакторены: локальные дубликаты удалены,
  утилиты импортируются из `shared/` (sys.path.insert по `__file__`);
- `day3/.env.example` — шаблон ключа теперь есть во всех днях.
Проверка: `py_compile` всех `.py` + запуск обоих Streamlit-приложений через
`streamlit.testing.AppTest` — без ошибок (реальные запросы к API не выполнялись).

## Известные проблемы и ограничения
1. **Нет истории диалога** ни в одном дне — каждый вызов API разовый.
2. **Частично устранено**: хелперы вынесены в `shared/`, но CSS-блок и часть
   UI-разметки day2 ↔ day3 по-прежнему дублируются.
3. **Частично устранено**: добавлены `README.md` и `day3/.env.example`; корневого
   `requirements.txt` и `pyproject.toml` по-прежнему нет.
4. **`deepseek-reasoner`** не полностью поддерживает параметры (temp/JSON) — это
   ограничение API, а не кода.
5. Обрезанный промпт Шага А мета-промпта всё равно используется (с warning).
6. Нет тестов/автопроверок — приложения проверяются вручную в браузере.

## Эволюция решений
- Консоль (Д1) → веб-UI Streamlit (Д2+) — ради наглядности демо/видео.
- OpenAI SDK (не прямой HTTP) — единый клиент на все дни.
- Параметры Дня 2 переиспользованы в Дне 3 как фундамент → преемственность дней.
- Git: один коммит на день (сообщения вида «Day N ... completed»); «наведение
  порядка» оформлено отдельным коммитом `5aa6898` «Day 3 Refactoring».

## Что дальше
- Выбрать тему Дня 4 (кандидаты в `activeContext.md`) и реализовать.
- Опционально: единый корневой `requirements.txt`, общий CSS/UI-модуль,
  добавление истории диалога (кандидат для Дня 4).
