# День 11 — Трёхслойная модель памяти агента (FastAPI + Streamlit + SQLite + tiktoken)

Приложение развивает день 10: единая история диалога заменена **тремя явными
слоями памяти** со своими таблицами и своим жизненным циклом —
краткосрочной (диалог текущей сессии), рабочей (данные текущей задачи) и
долговременной (профиль, предпочтения, решения, знания). Слоями заведует
`MemoryManager`, а агент при сборке контекста явно выбирает, что взять из
каждого слоя, и возвращает разбивку токенов по слоям.

Управление контекстом из дня 10 (четыре стратегии сборки, сжатие истории,
факты, ветвление) сохранено: стратегия решает, как собрать **краткосрочный**
слой, а рабочая и долговременная память подставляются блоками системного
сообщения независимо от неё.

## Управление контекстом

Атрибут агента `strategy` принимает одно из четырёх значений (`Enum Strategy`,
`backend/strategies.py`); сборка контекста — метод `Agent.prepare_context()`:

| Стратегия | Что уходит в DeepSeek | Сильная сторона |
|---|---|---|
| 🪟 `sliding_window` | системный промпт + последние `window_size` реплик | самый дешёвый и предсказуемый |
| 📌 `sticky_facts` | системный промпт (+факты «ключ: значение») + последние N | детали не теряются — факты хранятся в таблице `facts` |
| 🌿 `branching` | системный промпт + вся история активной ветки | ничего не теряет + ветвление диалога (`checkpoints`) |
| 🗜 `summary` | системный промпт (+конспект) + последние непокрытые реплики | сбалансированный компромисс (день 9) |

Переключатель — в сайдбаре («⚙️ Стратегия контекста»): выпадающий список
стратегии + слайдер `window_size`. Прогон сценария «собираем ТЗ» на каждой
стратегии с таблицей качества/стабильности/расхода токенов — в
[`comparison.md`](comparison.md).

## Модель памяти агента

Три слоя живут в трёх таблицах и различаются не только содержимым, но и ключом,
к которому привязаны:

| Слой | Таблица | Ключ | Что хранит | Когда очищается |
|---|---|---|---|---|
| 👤 Краткосрочная | `short_term_messages` | `session_id` | реплики текущего диалога (роль + текст + время) | `new_session()`, `DELETE /agents/{id}/memory/short-term`, `DELETE /agents/{id}/history` |
| 🗂 Рабочая | `working_memory` | `task_id` + `key` | данные активной задачи: цель, ограничения, решения, критерии | вручную (перезапись ключа) или сменой задачи; новая сессия её не трогает |
| 🧠 Долговременная | `long_term_memory` | `category` + `key` | профиль, устойчивые предпочтения, важные решения, знания | только вручную: `DELETE /agents/{id}/memory/long-term/{entry_id}` |

Контекст запроса собирается так: **системный промпт → блок рабочей памяти (все
ключи активной задачи) → блок долговременной памяти (релевантные записи, отбор
по ключевым словам с добором по уверенности) → конспект (если есть) → факты →
краткосрочный слой по стратегии → новый промпт**. Ответ `POST
/agents/{id}/generate` содержит поле `memory` с числом записей и токенов по
каждому слою; в UI перед диалогом его показывает индикатор «🧭 Что ушло в
последний запрос». Сравнение слоёв на сценарии «собираем ТЗ» — в
[`memory_layers_comparison.md`](memory_layers_comparison.md).

## Архитектура

```
Streamlit (порт 8501) ── HTTP (requests) ──► FastAPI (порт 8000) ──► DeepSeek API
   панели слоёв памяти, переключатель             │              https://api.deepseek.com
   задачи, кнопка «Новая сессия»                  ▼
                                    AgentManager (синглтон)
                                      └─ Agent × N
                                         ├─ session_id / task_id (активные)
                                         ├─ MemoryManager ──► три слоя памяти
                                         ├─ self.short_term_messages (зеркало сессии)
                                         ├─ подсчёт токенов (tiktoken)
                                         ├─ prepare_context() ──► стратегия:
                                         │     sliding_window / sticky_facts /
                                         │     branching / summary (ContextCompressor)
                                         └─ факты (facts) / ветки (checkpoints)
                                              │
                                              ▼
                                    SQLAlchemy → SQLite (day11/agents.db)
                                    таблицы: agents                (конфигурация + strategy/window_size,
                                                                    current_session_id, current_task_id)
                                             short_term_messages    (краткосрочная память: session_id)
                                             working_memory         (рабочая память: task_id + key)
                                             long_term_memory       (долговременная: category + key)
                                             summaries              (конспекты, append-only)
                                             token_usage            (метрики хода + токены по слоям)
                                             facts                  (факты sticky_facts)
                                             checkpoints            (снимки/ветки branching)
```

Поток одного сообщения: **пользователь → сборка блоков памяти и `prepare_context()`
по стратегии → аварийная обрезка при переполнении → API → метрики (включая
токены по слоям) в БД → пост-ходовое действие стратегии (сжатие / сохранение
фактов / снимок ветки) → ответ** (детали —
[docs/architecture.md](docs/architecture.md)).

## Структура

```
day11/
├── app.py               # Streamlit: панели трёх слоёв памяти, блок «Задача и сессия»,
│                        # переключатель стратегии, панели фактов/веток/токенов,
│                        # индикатор слоёв перед диалогом, кнопка тестового сценария
├── memory_layers_demo.py# сценарий «собираем ТЗ»: маршрутизация реплик по слоям,
│                        # доказательства после new_session(), запись отчёта
├── memory_layers_comparison.md # отчёт прогона: таблица слоёв и журнал 12 реплик
├── backend/
│   ├── config.py        # URL/дефолты, лимиты/цены, настройки сжатия, стратегии,
│   │                    # константы слоёв памяти (DEFAULT_TASK_ID, LONG_TERM_LIMIT)
│   ├── strategies.py    # Enum Strategy + AVAILABLE_STRATEGIES + strategy_from_value
│   ├── memory.py        # MemoryManager + MemoryCategory (Enum) + чистые render/подбор
│   ├── fact_extractor.py# эвристика извлечения фактов «ключ: значение»
│   ├── context_fsm.py   # стейт-машина сжатия: ContextState/ContextEvent (Enum) + State
│   ├── context_policy.py# чистая арифметика: когда сжимать, что оставить
│   ├── database.py      # SQLAlchemy: agents/short_term_messages/working_memory/
│   │                    # long_term_memory/summaries/token_usage/facts/checkpoints
│   ├── models.py        # Pydantic-схемы API (стратегии, ветки, факты, слои памяти)
│   ├── compressor.py    # ContextCompressor: план, суммаризация, запись конспекта (summary)
│   ├── agent.py         # Agent: session_id/task_id, слои памяти, токены, prepare_context
│   ├── agent_manager.py # AgentManager: пул, restore, стратегии, ветки, факты, слои памяти
│   └── main.py          # FastAPI: 29 эндпоинтов
├── tests/               # pytest: слои памяти, стратегии, факты, ветки, FSM,
│                        # хранилище, API (235 тестов)
├── docs/
│   ├── architecture.md  # компоненты, схема БД, слои памяти, стратегии, поток данных
│   ├── api.md           # эндпоинты с примерами и кодами ошибок
│   └── usage.md         # установка, запуск, слои памяти, стратегии/ветки, FAQ
├── comparison.md        # прогон «собираем ТЗ» на 4 стратегиях: таблица метрик
├── requirements.txt     # fastapi, uvicorn, streamlit, openai, requests, sqlalchemy,
│                        # tiktoken, httpx, pytest, pandas
└── .env.example         # шаблон ключа DEEPSEEK_API_KEY
```

## Быстрый старт

```bash
cd day11
python -m venv .venv
.venv/Scripts/python -m pip install -r requirements.txt
copy .env.example .env   # затем впишите DEEPSEEK_API_KEY=sk-...
```

Терминал 1 (бэкенд):

```bash
.venv/Scripts/python -m uvicorn backend.main:app --port 8000
```

Терминал 2 (фронтенд): откройте <http://localhost:8501>.

```bash
.venv/Scripts/python -m streamlit run app.py
```

Полная инструкция (ключ, слои памяти, переключение задач и стратегий, ветки,
тестовый сценарий, просмотр SQLite, FAQ) — в [docs/usage.md](docs/usage.md);
эндпоинты с примерами — в [docs/api.md](docs/api.md); Swagger — на
`http://127.0.0.1:8000/docs`.

## Тесты

```bash
cd day11
.venv/Scripts/python -m pytest -q      # 235 тестов
```

Тесты работают офлайн: клиент DeepSeek подменяется фейком (`tests/support.py`),
база — временная SQLite. Покрыты слои памяти (`MemoryManager`: сессии, upsert по
задаче и категории, отбор релевантных записей; `Agent`: блоки памяти в системном
сообщении, `new_session`, `set_task`, восстановление сессии/задачи из БД),
десять эндпоинтов `/memory/...`, а также `prepare_context` всех четырёх
стратегий, факты, ветки, FSM, политика сжатия, хранилище, компрессор и метрики.

Сценарий с фактами по слоям: `.venv/Scripts/python memory_layers_demo.py`
(пишет `memory_layers_comparison.md`, сеть не используется).

## Возможности

- **Три явных слоя памяти**: краткосрочная (сессия), рабочая (задача),
  долговременная (профиль/предпочтения/решения/знания) — отдельные таблицы и
  общий `MemoryManager`.
- **Разбивка контекста по слоям** в ответе `POST /agents/{id}/generate`
  (`memory.layers`: что использовано, сколько записей и токенов) и в UI —
  индикатор «🧭 Что ушло в последний запрос».
- **Панели слоёв в UI**: три вкладки — «👤 Краткосрочная» (реплики сессии +
  очистка), «🗂 Рабочая» (форма upsert-записи задачи), «🧠 Долговременная»
  (фильтр по категории, уверенность 0–1, удаление записи).
- **Задача и сессия в сайдбаре**: переключатель активной задачи, создание новой
  задачи, кнопка «🆕 Новая сессия» (обнуляет краткосрочный слой, сохраняя
  рабочую и долговременную память).
- **Четыре стратегии контекста**: `sliding_window`, `sticky_facts`, `branching`
  и `summary` (сжатие из дня 9); переключаются на живом агенте
  (`POST /agents/{id}/strategy`).
- **Sliding Window**: только последние `window_size` реплик + системный промпт.
- **Sticky Facts**: эвристика извлекает факты «ключ: значение» в таблицу `facts`
  (уникальность по `agent_id+key`), факты уходят в LLM блоком системного
  сообщения; панель фактов в UI.
- **Branching**: чекпоинты-снимки истории в таблице `checkpoints` (`parent_id`
  задаёт дерево), создание веток от текущего сообщения и переключение между ними.
- **Summary** (день 9): конспект вместо старых реплик, стейт-машина сжатия,
  экономия токенов (`saved_tokens` / `net_saved_tokens`).
- **Подсчёт токенов** (tiktoken) и таблица `token_usage` с режимом хода и
  расходом по слоям (`short_term_tokens` / `working_tokens` / `long_term_tokens`).
- **Тестовый сценарий «собираем ТЗ»**: кнопка «🎬 Запустить тестовый сценарий»
  прогоняет 12 реплик; результаты по стратегиям — в `comparison.md`, по слоям
  памяти — в `memory_layers_comparison.md`.
- Ошибки без traceback, при сбое генерации память не портится, UI жив при
  недоступном бэкенде (паттерн дней 6–10).

## Ограничения

- Один процесс бэкенда и один файл `day11/agents.db` (в `.gitignore` по `*.db`).
- `new_session()` удаляет данные, производные от старого диалога: реплики его
  сессии, конспекты (`summaries`) и факты (`facts`). Рабочая и долговременная
  память, снимки веток (`checkpoints`) и метрики (`token_usage`) сохраняются,
  то есть счётчик токенов и стоимость живут дольше одной сессии.
- Долговременные записи добавляются вручную (через UI или API) — LLM-извлечения
  профиля и предпочтений нет; переноса `facts` в долговременную память тоже нет
  (при необходимости их пишут в категорию `knowledge`).
- Отбор долговременных записей для контекста — эвристика по ключевым словам с
  добором по уверенности (детерминированная, без эмбеддингов).
- Активная ветка (branching) хранится в памяти и сбрасывается при рестарте
  бэкенда; дерево веток в `checkpoints` при этом сохраняется.
- Переключение ветки перезаписывает краткосрочный слой (`short_term_messages`)
  снимком выбранной ветки.
- Извлечение фактов — эвристика (`ключ: значение`), а не LLM-извлечение.
- Оценки tiktoken (`cl100k_base`) приблизительны: DeepSeek использует свой
  токенизатор; для запроса/ответа приоритет — фактические `usage` API.
- Лимиты контекста 8K/32K демонстрационные; правка — `MODEL_TOKEN_LIMITS` в
  `backend/config.py`.
- Суммаризация всегда идёт на `deepseek-chat` (temperature 0.2); при коротких
  репликах конспект может оказаться дороже заменяемых сообщений.
